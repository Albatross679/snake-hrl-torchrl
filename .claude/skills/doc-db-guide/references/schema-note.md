# Schema Reference

The full SQLite schema (all tables, columns, junction tables, and allowed values) is maintained in:

`/home/coder/nlp_Assignment3/.claude/skills/doc-db-query/references/schema.md`

Read that file when you need column names, types, or CHECK constraint values.
