---
phase: 02-data-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - figures/data_validation/summary_feature_histograms.png
  - figures/data_validation/action_histograms.png
  - figures/data_validation/outlier_counts.png
  - figures/data_validation/episode_length_distribution.png
  - figures/data_validation/step_index_distribution.png
  - figures/data_validation/action_coverage_per_dim.png
  - figures/data_validation/action_coverage_heatmap.png
  - data/surrogate/validation_report.md
autonomous: false
requirements:
  - DVAL-05

must_haves:
  truths:
    - "Validation script runs to completion on the full 27-batch dataset without errors"
    - "All 7 figures exist in figures/data_validation/ and are non-empty PNGs"
    - "validation_report.md exists in data/surrogate/ with pass/fail rubric table"
    - "Report contains actionable prose recommendations for surrogate training readiness"
    - "User has reviewed the report and figures and confirmed they are reasonable"
  artifacts:
    - path: "data/surrogate/validation_report.md"
      provides: "Structured validation report with pass/fail rubric and recommendations"
      contains: "PASS\\|WARN\\|FAIL"
    - path: "figures/data_validation/summary_feature_histograms.png"
      provides: "Distribution histograms for 4 summary features"
    - path: "figures/data_validation/action_histograms.png"
      provides: "Distribution histograms for 5 action dimensions"
    - path: "figures/data_validation/episode_length_distribution.png"
      provides: "Episode length distribution histogram"
    - path: "figures/data_validation/action_coverage_per_dim.png"
      provides: "Per-dimension action coverage with under-sampled bins highlighted"
  key_links:
    - from: "aprx_model_elastica/validate_data.py"
      to: "data/surrogate/batch_*.pt"
      via: "torch.load on all 27 batch files"
      pattern: "torch\\.load"
    - from: "aprx_model_elastica/validate_data.py"
      to: "data/surrogate/validation_report.md"
      via: "file write of generated markdown"
      pattern: "open.*validation_report"
---

<objective>
Execute the data validation script on the real collected dataset and verify the outputs are correct and useful.

Purpose: Plan 01 created the validation module; this plan runs it on real data to produce the actual validation artifacts (figures + report) and gets human confirmation that the analysis is reasonable and the go/no-go recommendation makes sense.

Output: 7 PNG figures in `figures/data_validation/`, validation report at `data/surrogate/validation_report.md`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-data-validation/02-CONTEXT.md
@.planning/phases/02-data-validation/02-01-SUMMARY.md

@aprx_model_elastica/validate_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run data validation on collected dataset</name>
  <files>figures/data_validation/, data/surrogate/validation_report.md</files>
  <action>
Run the validation script:
```bash
python3 -m aprx_model_elastica.validate_data --data-dir data/surrogate --fig-dir figures/data_validation --report-path data/surrogate/validation_report.md
```

After execution:
1. Verify all 7 expected figures exist in `figures/data_validation/` and are non-zero size
2. Verify `data/surrogate/validation_report.md` exists, is non-empty, and contains the pass/fail rubric table
3. Read the report and check that all sections are populated (not placeholder text)
4. If the script fails or produces incomplete output, diagnose and fix the issue in `validate_data.py`, then re-run

Expected runtime: 1-3 minutes (loading 2.3 GB of data, computing statistics, generating figures).
  </action>
  <verify>
    <automated>test -f data/surrogate/validation_report.md && test -f figures/data_validation/summary_feature_histograms.png && test -f figures/data_validation/action_histograms.png && test -f figures/data_validation/episode_length_distribution.png && test -f figures/data_validation/action_coverage_per_dim.png && echo "All outputs exist" || echo "MISSING outputs"</automated>
  </verify>
  <done>All 7 figures are non-empty PNGs in figures/data_validation/. validation_report.md exists with all sections populated. Script ran without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of validation report and figures</name>
  <files>data/surrogate/validation_report.md, figures/data_validation/</files>
  <action>
Present validation outputs to user for review. The following artifacts were generated:
- `data/surrogate/validation_report.md` — structured pass/fail rubric and recommendations
- `figures/data_validation/` — 7 diagnostic figures

What was built: Complete data validation analysis of the surrogate dataset (27 batch files, ~814K transitions). Generated 7 diagnostic figures and a structured validation report with pass/fail rubric and recommendations.

How to verify:
1. Open `data/surrogate/validation_report.md` — review the pass/fail rubric table at the top. Check that thresholds and statuses look reasonable.
2. Review the "Recommendations" section at the bottom — confirm the go/no-go assessment makes sense given the metrics.
3. Check figures in `figures/data_validation/`:
   - `summary_feature_histograms.png` — 4 histograms should show reasonable distributions (not all zeros, not degenerate)
   - `action_histograms.png` — 5 histograms should show coverage across [-1, 1] range
   - `action_coverage_per_dim.png` — should show which bins are well-covered vs sparse
   - `episode_length_distribution.png` — should show a distribution, not a single spike
4. Confirm the overall assessment: Is the dataset ready for surrogate training, or does it need more collection?

Resume signal: Type "approved" if the report and figures look good, or describe any issues (e.g., "outlier threshold too strict", "missing coverage in frequency dim")
  </action>
  <verify>
    <automated>echo "Checkpoint: requires human approval"</automated>
  </verify>
  <done>User has reviewed the validation report and figures and provided approval or feedback.</done>
</task>

</tasks>

<verification>
- All 7 figures exist and are viewable PNG images
- validation_report.md has structured pass/fail table with PASS/WARN/FAIL statuses
- Report has non-empty Recommendations section
- User has reviewed and approved the analysis
</verification>

<success_criteria>
- Script runs to completion without errors on the full dataset
- All figure files are non-empty
- Report contains pass/fail assessment and actionable recommendations
- User approves the validation quality
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-validation/02-02-SUMMARY.md`
</output>
