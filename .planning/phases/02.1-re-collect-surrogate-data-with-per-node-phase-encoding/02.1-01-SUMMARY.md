---
phase: 02.1-re-collect-surrogate-data-with-per-node-phase-encoding
plan: 01
subsystem: surrogate
tags: [state-encoding, phase-encoding, cpg, per-element, input-dim]

# Dependency graph
requires: []
provides:
  - encode_per_element_phase(action, t) -> (60,) float32 ndarray in aprx_model_elastica/state.py
  - encode_per_element_phase_batch(actions, t) -> (N, 60) float32 tensor in aprx_model_elastica/state.py
  - INPUT_DIM=189, PER_ELEMENT_PHASE_DIM=60 constants in aprx_model_elastica/state.py
  - tests/test_state_encoding.py with 5 verified test cases
affects:
  - 02.1-02 (collect_data.py uses encode_per_element_phase for dataset building)
  - 02.1-03 (dataset.py uses INPUT_DIM=189 and encode_per_element_phase_batch)
  - 03 (surrogate model training uses updated INPUT_DIM)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-element CPG phase features: [sin_j, cos_j, kappa_j] for j=0..19 at arc positions s_j in [0,1]"
    - "Action denormalization order: amplitude [0,5], frequency [0.5,3.0]Hz, wave_number [0.5,3.5], phase_offset [-pi,pi]"
    - "Output layout: [sin_0..sin_19, cos_0..cos_19, kappa_0..kappa_19] — 60-dim total"

key-files:
  created:
    - tests/test_state_encoding.py
  modified:
    - aprx_model_elastica/state.py

key-decisions:
  - "wave_number denorm range [0.5, 3.5] taken from perturb_rod_state() in collect_data.py — not a config param, hardcoded"
  - "TIME_ENC_DIM=2 deprecated with comment but kept for backward compat (old datasets/checkpoints)"
  - "INPUT_DIM updated from 131 to 189 — old models invalid; new dataset collection required"
  - "_ELEMENT_ARC_POSITIONS hardcoded via np.linspace(0,1,20) — no import from locomotion_elastica (avoids circular imports)"

patterns-established:
  - "Element arc positions: np.linspace(0.0, 1.0, NUM_ELEMENTS) — 20 elements in [0,1]"
  - "Batch variant mirrors numpy variant: same denorm math, torch ops, device-aware"

requirements-completed: [RCOL-01]

# Metrics
duration: 3min
completed: 2026-03-10
---

# Phase 02.1 Plan 01: Per-Element Phase Encoding Summary

**60-dim per-element CPG phase encoding (sin/cos/kappa per rod element) replaces 2-dim global time encoding; INPUT_DIM updated from 131 to 189 in state.py**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-10T13:31:08Z
- **Completed:** 2026-03-10T13:34:10Z
- **Tasks:** 1 (TDD: 2 commits — RED test + GREEN impl)
- **Files modified:** 2

## Accomplishments

- Added `encode_per_element_phase(action, t)` returning `(60,)` float32 ndarray — per-element `[sin, cos, kappa]` for all 20 rod elements
- Added `encode_per_element_phase_batch(actions, t)` returning `(N, 60)` float32 tensor for efficient training-time batch encoding
- Updated `INPUT_DIM` from 131 → 189 and added `PER_ELEMENT_PHASE_DIM=60` constant
- All 5 pytest tests pass; backward compat preserved (encode_phase, encode_serpenoid_time unchanged)

## Task Commits

Each task was committed atomically (TDD RED + GREEN):

1. **Task 1 RED: Failing tests** — `9ae22d2` (test)
2. **Task 1 GREEN: Implementation** — `8029b3c` (feat)

**Plan metadata:** committed with SUMMARY.md in final docs commit

_Note: TDD task produced two commits (test → feat) per TDD protocol_

## Files Created/Modified

- `/home/coder/snake-hrl-torchrl/aprx_model_elastica/state.py` — Added `PER_ELEMENT_PHASE_DIM`, updated `INPUT_DIM`, added arc position constants, `encode_per_element_phase()`, `encode_per_element_phase_batch()`
- `/home/coder/snake-hrl-torchrl/tests/test_state_encoding.py` — 5 tests: shape, batch shape, constants, formula correctness, backward compat

## Decisions Made

- **wave_number range `[0.5, 3.5]`:** Taken from `perturb_rod_state()` in `collect_data.py`, not a config parameter. Hardcoded to match physical parameter space used during data collection.
- **No import from locomotion_elastica:** `_ELEMENT_ARC_POSITIONS` hardcoded with `np.linspace(0,1,20)` to avoid circular imports. Comment documents the correspondence.
- **TIME_ENC_DIM kept:** Old constant deprecated with comment but preserved for backward compatibility with existing code paths.
- **INPUT_DIM breakage is intentional:** Old surrogate models trained with INPUT_DIM=131 are invalid. New dataset collection (Plan 02) and retraining required.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `encode_per_element_phase` and `INPUT_DIM=189` are the contract for Plan 02 (collect_data.py update) and Plan 03 (dataset.py update)
- Both downstream plans can now import `encode_per_element_phase` and `INPUT_DIM` from `aprx_model_elastica.state`
- No blockers.

---
*Phase: 02.1-re-collect-surrogate-data-with-per-node-phase-encoding*
*Completed: 2026-03-10*
