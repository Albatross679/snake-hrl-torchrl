---
phase: 03-train-surrogate-model-using-supervised-learning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aprx_model_elastica/train_surrogate.py
  - aprx_model_elastica/dataset.py
  - aprx_model_elastica/sweep.py
  - aprx_model_elastica/arch_sweep.py
  - .planning/phases/03-train-surrogate-model-using-supervised-learning/03-CONTEXT.md
autonomous: true
requirements: [SURR-01, SURR-02, ARCH-01, ARCH-02, ARCH-03, ARCH-04, ARCH-05]

must_haves:
  truths:
    - "train_surrogate.py loads Phase 02.2 flat-format data via FlatStepDataset"
    - "All 6 sweep configs complete training without crash on data/surrogate_rl_step/"
    - "Each completed run has model.pt, normalizer.pt, config.json, metrics.json in its output dir"
    - "W&B logs per-epoch train/val loss and per-component losses for all runs"
    - "ResidualSurrogateModel and HistorySurrogateModel forward passes work (ARCH-01, ARCH-02)"
    - "CLI args --rollout-weight, --rollout-steps, --use-residual, --history-k wire to config (ARCH-04)"
    - "arch_sweep.py dry-run completes without error (ARCH-05)"
  artifacts:
    - path: "aprx_model_elastica/train_surrogate.py"
      provides: "Training loop using FlatStepDataset for Phase 02.2 data"
      contains: "FlatStepDataset"
    - path: "aprx_model_elastica/dataset.py"
      provides: "FlatStepDataset with deltas property and serpenoid_time alias"
      contains: "self.deltas"
    - path: "aprx_model_elastica/sweep.py"
      provides: "6-config parallel sweep runner"
      contains: "SWEEP_CONFIGS"
    - path: "aprx_model_elastica/arch_sweep.py"
      provides: "Architecture experiment sweep runner with --dry-run support"
      contains: "ARCH_SWEEP_CONFIGS"
    - path: "output/surrogate/sweep_summary.json"
      provides: "Sweep results with best_val_loss per run"
  key_links:
    - from: "aprx_model_elastica/train_surrogate.py"
      to: "aprx_model_elastica/dataset.py"
      via: "FlatStepDataset import and instantiation"
      pattern: "FlatStepDataset.*data_dir"
    - from: "aprx_model_elastica/sweep.py"
      to: "aprx_model_elastica/train_surrogate.py"
      via: "subprocess launch with CLI args"
      pattern: "train_surrogate"
    - from: "aprx_model_elastica/arch_sweep.py"
      to: "aprx_model_elastica/train_surrogate.py"
      via: "subprocess launch with architecture CLI args"
      pattern: "train_surrogate.*--use-residual"
---

<objective>
Wire FlatStepDataset into the training pipeline, create arch_sweep.py for ARCH-05 coverage, update CONTEXT.md to reflect the replan rationale, and run the full 6-config sweep on Phase 02.2 data.

Purpose: train_surrogate.py currently imports SurrogateDataset (Phase 02.1 checkpoint format) which is incompatible with Phase 02.2 flat format. FlatStepDataset exists but needs minor additions (deltas property, serpenoid_time key alias) before the training loop can use it. Additionally, arch_sweep.py (required for ARCH-05) does not exist on disk and must be created. CONTEXT.md must be updated to document why rollout_weight is set to 0.0 for all configs (Phase 02.2 flat data has no contiguous trajectories).

Output: All 6 sweep runs complete with metrics.json, sweep_summary.json generated. arch_sweep.py exists with --dry-run support.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-train-surrogate-model-using-supervised-learning/03-CONTEXT.md

@aprx_model_elastica/train_surrogate.py
@aprx_model_elastica/dataset.py
@aprx_model_elastica/sweep.py
@aprx_model_elastica/model.py
@aprx_model_elastica/state.py
@tests/test_surrogate_arch.py

<interfaces>
<!-- Key incompatibilities to fix -->

1. train_surrogate.py line 22 imports SurrogateDataset — must change to FlatStepDataset
2. train_surrogate.py line 266-269 constructs SurrogateDataset — must change to FlatStepDataset
3. train_surrogate.py line 307 accesses train_dataset.deltas — FlatStepDataset has no .deltas attribute (computes delta on-the-fly in __getitem__). Must add self.deltas = self.next_states - self.states to FlatStepDataset.__init__() after the train/val mask is applied.
4. compute_single_step_loss() line 113 reads batch["serpenoid_time"] — FlatStepDataset.__getitem__ returns key "t_start" not "serpenoid_time". Fix by adding "serpenoid_time": self.t_starts[idx] alias to FlatStepDataset.__getitem__().
5. TrajectoryDataset (used for rollout loss) wraps SurrogateDataset internally (line 415) and expects Phase 02.1 format. Phase 02.2 data has steps_per_run=1 (independent transitions, no contiguous trajectories). Rollout loss CANNOT work with Phase 02.2 data. All rollout_weight values in SWEEP_CONFIGS should be set to 0.0.

<!-- Existing ARCH symbols (VERIFIED on disk) -->
- model.py:ResidualSurrogateModel (line 149) — ARCH-01 covered
- model.py:HistorySurrogateModel (line 262) — ARCH-02 covered
- train_surrogate.py --rollout-weight CLI arg (line 55) — ARCH-04 covered
- tests/test_surrogate_arch.py — ARCH-01..04 tests exist and pass
- arch_sweep.py — DOES NOT EXIST on disk, must be created for ARCH-05

<!-- FlatStepDataset interface (from dataset.py) -->
class FlatStepDataset(Dataset):
    # __init__(data_dir, split, val_fraction, seed)
    # Attributes: states (N,124), next_states (N,124), actions (N,5), t_starts (N,), episode_ids (N,), forces (dict or None)
    # __getitem__ returns: {state, action, t_start, next_state, delta, forces}
    # get_sample_weights(n_bins, clip_max) -> (N,) weights

<!-- SurrogateDataset interface (being replaced) -->
class SurrogateDataset(Dataset):
    # Attributes: states, next_states, actions, serpenoid_times, deltas, episode_ids, step_indices
    # __getitem__ returns: {state, action, serpenoid_time, next_state, delta}
    # get_sample_weights(n_bins, clip_max) -> (N,) weights
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire FlatStepDataset, create arch_sweep.py, update CONTEXT.md</name>
  <files>aprx_model_elastica/dataset.py, aprx_model_elastica/train_surrogate.py, aprx_model_elastica/sweep.py, aprx_model_elastica/arch_sweep.py, .planning/phases/03-train-surrogate-model-using-supervised-learning/03-CONTEXT.md</files>
  <action>
Four areas of work:

**A. dataset.py — Add missing attributes to FlatStepDataset:**
1. After the train/val mask is applied (after line 579), add:
   `self.deltas = self.next_states - self.states`
   This is needed by train_surrogate.py line 307 (`normalizer.fit(train_dataset.states, train_dataset.deltas)`).
2. In `__getitem__`, add a `"serpenoid_time"` key that aliases `self.t_starts[idx]`:
   Change the return dict to include BOTH `"t_start"` (for backward compat) AND `"serpenoid_time"` (for training loop compat):
   ```python
   "serpenoid_time": self.t_starts[idx],  # alias for training loop compat
   ```

**B. train_surrogate.py — Switch from SurrogateDataset to FlatStepDataset:**
1. Change import on line 22: `from aprx_model_elastica.dataset import FlatStepDataset, TrajectoryDataset`
2. Change lines 266-269 to use FlatStepDataset:
   ```python
   train_dataset = FlatStepDataset(config.data_dir, split="train", val_fraction=config.val_fraction)
   val_dataset = FlatStepDataset(config.data_dir, split="val", val_fraction=config.val_fraction)
   ```
3. For the rollout loss block (lines 368-392): wrap TrajectoryDataset construction in a try/except that catches any error and sets `config.rollout_loss_weight = 0` with a printed warning. This handles Phase 02.2 data gracefully without crashing.

**C. sweep.py — Set all rollout_weight values to 0.0 and reduce to 6 configs:**
Phase 02.2 has steps_per_run=1 (independent transitions). TrajectoryDataset cannot build contiguous windows from this data. Set `"rollout_weight": 0.0` for ALL configs in SWEEP_CONFIGS. This means:
- The 5 LR/size sweep configs: change rollout_weight from 0.1 to 0.0
- A1: already 0.0 — but now identical to sweep_lr1e3_h512x3. REMOVE A1.
- A3/A4/A5: change to 0.0 — now meaningless without rollout variation. REMOVE A3, A4, A5.
- B1 (residual): change rollout_weight from 0.1 to 0.0.

After removing A1/A3/A4/A5, the sweep has 6 unique configs: 5 LR/size + B1 residual.

Update the module docstring to reflect the simplified sweep.

**D. Create aprx_model_elastica/arch_sweep.py (ARCH-05):**
This file does not exist on disk. Create it as an architecture experiment sweep runner, following the same pattern as sweep.py. It should:
1. Define ARCH_SWEEP_CONFIGS list with architecture variant configs (at minimum: B1 residual with --use-residual flag, and optionally a baseline for comparison). All configs use rollout_weight=0.0 (Phase 02.2 data).
2. Accept CLI args: --data-dir, --device, --epochs, --output-base, --dry-run
3. When --dry-run is passed: run each config for 1 epoch only and exit (for testing)
4. Launch configs via subprocess calling `python3 -m aprx_model_elastica.train_surrogate` with appropriate CLI args (same pattern as sweep.py)
5. Include `if __name__ == "__main__"` block for `python3 -m aprx_model_elastica.arch_sweep` invocation

Note: In this replanned phase, the main sweep.py already includes B1 residual. arch_sweep.py exists primarily to satisfy ARCH-05 (the requirement that an arch sweep runner exists with --dry-run support). It can be a lightweight wrapper that references the architecture configs.

**E. Update 03-CONTEXT.md decisions section:**
Add a "Replan rationale" subsection under decisions documenting:
- Phase 02.2 flat data has steps_per_run=1 (independent transitions, no contiguous trajectories)
- TrajectoryDataset cannot build windows from this data, so rollout loss is disabled (rollout_weight=0.0 for all configs)
- Experiment A configs (A1/A3/A4/A5) removed — they varied only rollout_weight which is now forced to 0.0
- Sweep reduced from 10 configs to 6 (5 LR/size + B1 residual)
- "rw in {0.0, 0.1, 0.3, 0.5}" decision superseded — all configs use rw=0.0
- "B1 rollout_w=0.1" decision superseded — B1 uses rw=0.0
  </action>
  <verify>
    <automated>python3 -c "
from aprx_model_elastica.dataset import FlatStepDataset
ds = FlatStepDataset('data/surrogate_rl_step', split='train')
item = ds[0]
assert hasattr(ds, 'deltas'), 'FlatStepDataset missing deltas attribute'
assert 'serpenoid_time' in item, 'Missing serpenoid_time key in __getitem__'
assert ds.deltas.shape == ds.states.shape, 'deltas shape mismatch'
print(f'OK: {len(ds)} transitions, deltas shape={ds.deltas.shape}, keys={list(item.keys())}')
" && python3 -c "
from aprx_model_elastica.sweep import SWEEP_CONFIGS
assert len(SWEEP_CONFIGS) == 6, f'Expected 6 configs, got {len(SWEEP_CONFIGS)}'
for cfg in SWEEP_CONFIGS:
    assert cfg['rollout_weight'] == 0.0, f'{cfg[\"name\"]} has rollout_weight={cfg[\"rollout_weight\"]}'
print(f'OK: {len(SWEEP_CONFIGS)} configs, all rollout_weight=0.0')
" && python3 -m pytest tests/test_surrogate_arch.py::test_residual_model_forward tests/test_surrogate_arch.py::test_history_model_forward tests/test_surrogate_arch.py::test_train_cli_args -x -q && python3 -m aprx_model_elastica.arch_sweep --data-dir data/surrogate_rl_step --epochs 1 --dry-run --output-base /tmp/arch_sweep_dryrun</automated>
  </verify>
  <done>FlatStepDataset has .deltas and serpenoid_time alias. train_surrogate.py uses FlatStepDataset. sweep.py has 6 configs all with rollout_weight=0.0. arch_sweep.py exists with --dry-run support (ARCH-05). CONTEXT.md updated with replan rationale. ARCH-01..04 tests pass. ARCH-05 dry-run completes.</done>
</task>

<task type="auto">
  <name>Task 2: Launch full sweep on Phase 02.2 data</name>
  <files>output/surrogate/sweep_summary.json</files>
  <action>
1. Clear old sweep output directories (they contain results from Phase 02.1 data):
   ```bash
   rm -rf output/surrogate/sweep_lr* output/surrogate/arch_* output/surrogate/sweep_summary.json output/surrogate/sweep.log
   ```

2. Launch the sweep in a tmux session so it runs in background:
   ```bash
   tmux new-session -d -s sweep 'cd /home/coder/snake-hrl-torchrl && python3 -m aprx_model_elastica.sweep --data-dir data/surrogate_rl_step --device cuda --epochs 200 --output-base output/surrogate 2>&1 | tee output/surrogate/sweep.log'
   ```

3. Wait 30 seconds, then verify the sweep is alive by checking:
   a. The tmux session exists: `tmux has-session -t sweep 2>/dev/null && echo "tmux session alive"`
   b. At least one run directory was created: `ls output/surrogate/sweep_lr* 2>/dev/null`
   c. The sweep log file has recent output: `tail -3 output/surrogate/sweep.log`
   If the tmux session does not exist or the log shows an error/traceback, the sweep crashed — diagnose and fix before proceeding.

4. Print monitoring instructions for the user:
   - `tmux attach -t sweep` to watch live output
   - `tail -f output/surrogate/sweep.log` to monitor progress
   - Expected runtime: ~7-8 hours for 6 configs at 200 epochs each on V100 (parallel)

Note: This task launches training in background. Plan 02 depends on sweep completion. The user should wait for the tmux session to finish before running Plan 02.
  </action>
  <verify>
    <automated>sleep 30 && tmux has-session -t sweep 2>/dev/null && echo "TMUX_OK" && ls output/surrogate/sweep_lr1e4_h256x3 output/surrogate/sweep_lr1e3_h512x3 output/surrogate/arch_B1_residual 2>/dev/null | wc -l && tail -3 output/surrogate/sweep.log 2>/dev/null | grep -v "Traceback\|Error" | head -1</automated>
  </verify>
  <done>All 6 sweep configs launched in parallel via tmux. Tmux session confirmed alive. Run directories created and log file shows active training output. User can monitor via tmux attach -t sweep.</done>
</task>

</tasks>

<verification>
- FlatStepDataset loads Phase 02.2 data correctly with .deltas and serpenoid_time alias
- All existing arch tests pass (ARCH-01..04): python3 -m pytest tests/test_surrogate_arch.py -x -q
- arch_sweep.py --dry-run completes without error (ARCH-05)
- Sweep launched and producing output in output/surrogate/
- CONTEXT.md updated with replan rationale for rollout_weight=0.0
</verification>

<success_criteria>
- train_surrogate.py uses FlatStepDataset (not SurrogateDataset)
- sweep.py has exactly 6 configs, all with rollout_weight=0.0 (Phase 02.2 has no trajectory data)
- arch_sweep.py exists with ARCH_SWEEP_CONFIGS and --dry-run support (ARCH-05)
- ARCH-01..04 tests pass, ARCH-05 dry-run succeeds
- CONTEXT.md documents replan rationale (rollout_weight forced to 0.0, A configs removed)
- All sweep runs launched in parallel and producing model checkpoints
- Each completed run directory has: model.pt, normalizer.pt, config.json, metrics.json
</success_criteria>

<output>
After completion, create `.planning/phases/03-train-surrogate-model-using-supervised-learning/03-01-SUMMARY.md`
</output>
