---
phase: 03-train-surrogate-model-using-supervised-learning
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - script/launch_sweep.sh
  - aprx_model_elastica/train_surrogate.py
autonomous: true
requirements: [SURR-02]

must_haves:
  truths:
    - "All 15 sweep configs complete successfully with W&B logging (no deadlocks)"
    - "Each run produces model.pt, normalizer.pt, config.json, and metrics.json in its output directory"
    - "Sweep runs sequentially in tmux (not parallel) to avoid GPU contention"
    - "DataLoader uses persistent_workers=True to prevent semaphore deadlock"
  artifacts:
    - path: "script/launch_sweep.sh"
      provides: "tmux launch script for 15-config sweep"
    - path: "output/surrogate/sweep_summary.json"
      provides: "Ranked results from all 15 configs (created automatically by sweep.py after all runs complete)"
  key_links:
    - from: "script/launch_sweep.sh"
      to: "aprx_model_elastica/sweep.py"
      via: "python -m aprx_model_elastica.sweep invocation"
      pattern: "aprx_model_elastica.sweep"
---

<objective>
Fix the DataLoader semaphore deadlock (persistent_workers=True), clean up the failed first attempt, smoke-test with 1-epoch runs, then relaunch the full 15-config sweep in tmux.

Purpose: Execute the actual training sweep that produces model checkpoints for analysis in Plan 03.
Output: Running sweep in tmux session, 15 output directories with checkpoints.

Note: First sweep attempt (2026-03-11) failed — M1 deadlocked at epoch 15 due to DataLoader semaphore leak. Fix applied, sweep must be relaunched from scratch.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-train-surrogate-model-using-supervised-learning/03-CONTEXT.md
@.planning/phases/03-train-surrogate-model-using-supervised-learning/03-01-SUMMARY.md

@aprx_model_elastica/sweep.py
@aprx_model_elastica/train_surrogate.py
</context>

<tasks>

<task type="auto">
  <name>Task 0: Fix DataLoader semaphore deadlock</name>
  <files>aprx_model_elastica/train_surrogate.py</files>
  <action>
**Background:** The first sweep attempt (launched 2026-03-11) hit a DataLoader multiprocessing deadlock. M1 trained to epoch 15 (val_loss=0.813) then hung at 0% CPU/GPU due to semaphore leaks. See issues/dataloader-semaphore-deadlock.md.

1. Verify `persistent_workers=True` is set on ALL 4 DataLoader instances in train_surrogate.py (lines ~479, 484, 488, 537). This fix was already applied — confirm it's present.

2. Kill the running sweep session if still active:
   ```bash
   tmux kill-session -t gsd-sweep 2>/dev/null
   ```

3. Clean up incomplete M1/M2 output directories from the failed sweep:
   ```bash
   rm -rf output/surrogate/M1 output/surrogate/M2 output/surrogate/M1.log output/surrogate/M2.log
   rm -f output/surrogate/sweep_full.log
   ```
  </action>
  <verify>
    <automated>grep -c "persistent_workers=True" aprx_model_elastica/train_surrogate.py</automated>
  </verify>
  <done>persistent_workers=True confirmed on all DataLoaders, old sweep cleaned up</done>
</task>

<task type="auto">
  <name>Task 1: Smoke test sweep with 1-epoch runs</name>
  <files>aprx_model_elastica/sweep.py</files>
  <action>
1. Run `python3 -m aprx_model_elastica.sweep --dry-run` — confirm 15 configs print correctly.

2. Run a single 1-epoch smoke test for ONE config from each architecture family to verify end-to-end:
   - MLP: `python3 -m aprx_model_elastica.train_surrogate --data-dir data/surrogate_rl_step --epochs 1 --arch mlp --hidden-dims 256,256,256 --lr 1e-4 --save-dir /tmp/smoke_mlp --save-best-val-loss --device cuda --auto-batch`
   - Residual: `python3 -m aprx_model_elastica.train_surrogate --data-dir data/surrogate_rl_step --epochs 1 --arch residual --hidden-dims 512,512,512 --lr 3e-4 --save-dir /tmp/smoke_residual --save-best-val-loss --device cuda --auto-batch`
   - Transformer: `python3 -m aprx_model_elastica.train_surrogate --data-dir data/surrogate_rl_step --epochs 1 --arch transformer --n-layers 4 --n-heads 4 --d-model 128 --lr 3e-4 --save-dir /tmp/smoke_transformer --save-best-val-loss --device cuda --auto-batch`

3. Verify each smoke test produced model.pt, normalizer.pt, config.json, metrics.json.

4. Validate smoke test outputs contain valid metrics: load each metrics.json, confirm it has "val_loss" key with a finite float value, and confirm model.pt file size > 1KB.

5. Fix any errors before proceeding to full sweep.
  </action>
  <verify>
    <automated>python3 -c "
import os, json
for a in ['mlp','residual','transformer']:
    d = f'/tmp/smoke_{a}'
    assert os.path.exists(f'{d}/metrics.json'), f'{a}: missing metrics.json'
    assert os.path.getsize(f'{d}/model.pt') > 1024, f'{a}: model.pt too small'
    m = json.load(open(f'{d}/metrics.json'))
    assert 'val_loss' in m or any('val_loss' in str(k) for k in m), f'{a}: no val_loss in metrics'
print('All 3 smoke tests: files exist, model.pt valid, metrics.json has val_loss')
"</automated>
  </verify>
  <done>1-epoch smoke tests pass for all 3 architecture families (MLP, Residual, Transformer), each producing model.pt + metrics.json with valid val_loss values</done>
</task>

<task type="auto">
  <name>Task 2: Launch full 15-config sweep in tmux</name>
  <files>script/launch_sweep.sh</files>
  <action>
1. Ensure sweep.py runs configs sequentially (per CONTEXT.md locked decision: "Sweep runs sequentially in tmux -- sweep.py handles via subprocess"). Verify sweep.py uses subprocess.run() (blocking) NOT subprocess.Popen() (parallel). If it uses Popen, change to subprocess.run() so configs execute one at a time, avoiding GPU contention on V100-16GB. This is NOT optional -- sequential execution is the locked decision.

2. Create script/launch_sweep.sh:
```bash
#!/bin/bash
# Launch 15-config surrogate sweep in tmux session
# Runs sequentially via sweep.py (subprocess.run per config)
# Monitor: tmux attach -t gsd-sweep
# Logs: output/surrogate/*.log

set -e
cd /home/coder/snake-hrl-torchrl

python3 -m aprx_model_elastica.sweep \
  --data-dir data/surrogate_rl_step \
  --device cuda \
  --epochs 200 \
  --output-base output/surrogate

echo "Sweep complete. Results: output/surrogate/sweep_summary.json"
```

3. chmod +x script/launch_sweep.sh

4. Launch in tmux:
   `tmux new-session -d -s gsd-sweep 'bash script/launch_sweep.sh 2>&1 | tee output/surrogate/sweep_full.log'`

5. Verify tmux session is running: `tmux has-session -t gsd-sweep`

NOTE: sweep.py writes output/surrogate/sweep_summary.json automatically after all 15 runs complete, containing ranked results by val_loss.
  </action>
  <verify>
    <automated>tmux has-session -t gsd-sweep 2>/dev/null && echo "Sweep session running" || echo "ERROR: No tmux session"</automated>
  </verify>
  <done>15-config sweep launched in tmux session gsd-sweep running sequentially, logs streaming to output/surrogate/sweep_full.log</done>
</task>

</tasks>

<verification>
1. `tmux has-session -t gsd-sweep` — session exists
2. `ls output/surrogate/*.log | wc -l` — shows log files being created
3. After sweep completes: `python3 -c "import json; d=json.load(open('output/surrogate/sweep_summary.json')); print(f'Best: {d[\"best_run\"]} val_loss={d[\"best_val_loss\"]:.6f}')"` — prints best run
</verification>

<success_criteria>
- Smoke tests pass for all 3 architecture families (MLP, Residual, Transformer) with valid metrics
- sweep.py confirmed to run configs sequentially via subprocess.run()
- Full 15-config sweep launched in tmux session gsd-sweep
- Sweep produces output directories and log files for all 15 configs
- After completion: sweep_summary.json contains ranked results (created by sweep.py)
</success_criteria>

<output>
After completion, create `.planning/phases/03-train-surrogate-model-using-supervised-learning/03-02-SUMMARY.md`
</output>
