---
phase: 03-train-surrogate-model-using-supervised-learning
plan: 05
type: execute
wave: 3
depends_on:
  - 03-04
files_modified:
  - output/surrogate/best/model.pt
  - output/surrogate/best/normalizer.pt
  - output/surrogate/best/config.json
  - output/surrogate/best/selection.json
  - experiments/surrogate-architecture-experiments-results.md
  - .planning/STATE.md
autonomous: false
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - ARCH-05

must_haves:
  truths:
    - "Human has reviewed arch_sweep_summary.json and compared results to the Phase 02.2 baseline val_loss from Plan 01 sweep (replaces Phase 02.1 baseline of 0.2161)"
    - "Best architecture for Phase 4 is selected and its checkpoint is at output/surrogate/best/"
    - "If A+B fail to improve omega_z R² above 0.4, Experiment C (history window) is run or explicitly deferred"
    - "Experiment results are documented in experiments/surrogate-architecture-experiments-results.md"
    - "STATE.md records the selected architecture and checkpoint path"
  artifacts:
    - path: "output/surrogate/best/model.pt"
      provides: "Best surrogate checkpoint for Phase 4 validation"
    - path: "output/surrogate/best/config.json"
      provides: "Architecture config for the selected model"
    - path: "output/surrogate/best/normalizer.pt"
      provides: "Normalization statistics fitted on training data"
    - path: "experiments/surrogate-architecture-experiments-results.md"
      provides: "Experiment record: metrics table, comparison to baseline, selection rationale"
  key_links:
    - from: "output/surrogate/best/"
      to: "Phase 4 validate.py"
      via: "Phase 4 plan reads checkpoint from output/surrogate/best/"
      pattern: "output/surrogate/best"
    - from: "experiments/surrogate-architecture-experiments-results.md"
      to: "STATE.md decisions"
      via: "Selected architecture recorded in STATE.md Decisions section"
      pattern: "Phase 3.*selected"
---

<objective>
Analyze the arch sweep results, make a human-reviewed architecture selection decision, optionally run Experiment C (history window) if A+B underperform, copy the winning checkpoint to output/surrogate/best/, and document findings. This plan gates Phase 4 from starting until the best architecture is confirmed.

Purpose: Phase 4 (trajectory validation) needs a single confirmed checkpoint. This plan ensures the selection is made deliberately, with a human reviewing the tradeoffs between val_loss, rollout_loss, and omega_z R².

Output: output/surrogate/best/ populated, experiment results documented, STATE.md updated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-train-surrogate-model-using-supervised-learning/03-RESEARCH.md
@.planning/phases/03-train-surrogate-model-using-supervised-learning/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze sweep results and write experiment report</name>
  <files>experiments/surrogate-architecture-experiments-results.md</files>
  <action>
    Read output/surrogate/arch_sweep/arch_sweep_summary.json and per-run metrics.json files. Compute a decision table comparing all variants against the Plan 01 baseline (val_loss from output/surrogate/sweep_lr1e3_h512x3/metrics.json — re-trained on Phase 02.2 data, NOT the old 0.2161 from Phase 02.1):

    Table columns: Run, val_loss, vs_baseline (delta), rollout_steps, rollout_weight, arch, status

    Include the baseline row (arch_A2_rw0.1_BASELINE from sweep_lr1e3_h512x3/metrics.json — this is the Phase 02.2 re-trained baseline; there is no hardcoded fallback since Plan 01 must complete before Plan 05).

    If W&B component losses are available in any metrics.json or logged artifacts, extract omega_z component loss to assess whether it improved.

    Apply the RESEARCH.md decision rule:
    - If best variant achieves val_loss < 0.21 → clear winner identified
    - If no variant improves over the Plan 01 baseline by >1% → note whether Experiment C should run
    - Tie-breaking: within 0.002 val_loss, prefer lower rollout_loss

    Write experiments/surrogate-architecture-experiments-results.md with:
    - frontmatter: type, status, created, updated, description, tags
    - Full results table
    - Decision recommendation: which run to approve and why
    - Whether Experiment C (history window K=2) is recommended
    - Estimated omega_z improvement if data is available
  </action>
  <verify>
    <automated>python3 -c "
import json
from pathlib import Path
p = Path('output/surrogate/arch_sweep/arch_sweep_summary.json')
if p.exists():
    d = json.load(open(p))
    print(f'Sweep complete. Best: {d[\"best_run\"]} = {d[\"best_val_loss\"]:.6f}')
    print(f'Baseline: {d[\"baseline\"][\"val_loss\"]:.6f}')
    n = len(d.get('results', []))
    print(f'Runs: {n}')
else:
    print('MISSING: arch_sweep_summary.json — sweep may not be complete yet')
"
    </automated>
  </verify>
  <done>experiments/surrogate-architecture-experiments-results.md written with full results table, decision recommendation, and Experiment C recommendation (yes/no/defer)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human reviews results and selects architecture</name>
  <action>
    Present the results from experiments/surrogate-architecture-experiments-results.md to the user for review. The user must select one of three options before Task 3 can proceed.
  </action>
  <verify>
    <automated>echo "Human review required — see experiments/surrogate-architecture-experiments-results.md"</automated>
  </verify>
  <done>Human has replied with approve {run_name}, approve baseline, or run experiment-c</done>

  <what-built>Arch sweep results table and selection recommendation in experiments/surrogate-architecture-experiments-results.md. Analysis covers Experiments A (4 rollout weight variants) and B (residual MLP). Recommendation states which run to promote to output/surrogate/best/.</what-built>
  <how-to-verify>
    1. Read the report: cat experiments/surrogate-architecture-experiments-results.md
    2. Compare val_loss values to the Plan 01 Phase 02.2 baseline (from sweep_lr1e3_h512x3/metrics.json) — did any variant improve?
    3. Check if omega_z prediction improved (W&B component/omega_z metric if available)
    4. Review the recommendation:
       - Is the recommended run the best by val_loss?
       - If val_loss improved but is still above 0.20, is that enough for Phase 4?
       - Should Experiment C (history window) be run, or is A+B sufficient?
    5. Check output/surrogate/arch_sweep/ to verify all 5 runs completed
  </how-to-verify>
  <resume-signal>
    Reply with ONE of:
    - "approve {run_name}" — promote that checkpoint to output/surrogate/best/ (e.g., "approve arch_A3_rw0.3")
    - "approve baseline" — use the Plan 01 checkpoint (sweep_lr1e3_h512x3) as-is
    - "run experiment-c" — trigger Experiment C (history window K=2) before selecting
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Copy winning checkpoint to output/surrogate/best/ and update STATE.md</name>
  <files>output/surrogate/best/model.pt, output/surrogate/best/normalizer.pt, output/surrogate/best/config.json, output/surrogate/best/selection.json, .planning/STATE.md</files>
  <action>
    Based on the human's selection signal from Task 2:

    **If "approve {run_name}":**
    - Source dir: output/surrogate/arch_sweep/{run_name}/
    - Use shutil.copy2() to copy model.pt, normalizer.pt, config.json to output/surrogate/best/
    - Read val_loss from output/surrogate/arch_sweep/{run_name}/metrics.json
    - Write output/surrogate/best/selection.json:
      {"selected_run": run_name, "val_loss": val_loss, "phase": "03",
       "reason": "Best val_loss among A+B arch experiments",
       "baseline_val_loss": baseline_val_loss,  # read from sweep_lr1e3_h512x3/metrics.json
       "improvement": baseline_val_loss - val_loss}

    **If "approve baseline":**
    - Source dir: output/surrogate/sweep_lr1e3_h512x3/
    - Copy model.pt, normalizer.pt, config.json to output/surrogate/best/
    - Write selection.json with reason "No experiment improved over Plan 01 baseline; using original 512x3 checkpoint"

    **If "run experiment-c":**
    - First implement the HistorySurrogateModel training loop (the train_surrogate.py TODO from Plan 03):
      - When config.history_k > 0, replace the single-step SurrogateDataset loader with a HistoryDataset loader
      - The single-step loss uses only the last step's delta from the history window
      - Rollout loss is disabled for history training (gradient flow through K-step history is already expensive)
    - Run training in tmux: python3 -m aprx_model_elastica.train_surrogate --lr 1e-3 --hidden-dims 512,512,512 --history-k 2 --rollout-weight 0.0 --save-dir output/surrogate/arch_sweep/arch_C1_history_k2 --wandb --run-name arch_C1_history_k2 --epochs 200 --save-best-val-loss
    - After it completes (~90 min), re-run Task 1 analysis, then wait for human approval again

    **STATE.md update** (in all final-selection cases): Append to Decisions section:
      `- [Phase 03]: Selected architecture = {run_name}, val_loss={val_loss:.4f}, checkpoint=output/surrogate/best/`

    Create output/surrogate/best/ with Path.mkdir(parents=True, exist_ok=True) before copying.
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
import json
best = Path('output/surrogate/best')
files = ['model.pt', 'normalizer.pt', 'config.json', 'selection.json']
missing = [f for f in files if not (best / f).exists()]
if missing:
    print(f'MISSING in output/surrogate/best/: {missing}')
else:
    sel = json.load(open(best / 'selection.json'))
    print(f'Selected: {sel[\"selected_run\"]}')
    print(f'val_loss: {sel[\"val_loss\"]:.6f}')
    print(f'baseline: {sel[\"baseline_val_loss\"]:.6f}')
    impr = sel.get(\"improvement\", 0)
    print(f'improvement: {impr:+.6f}')
"
    </automated>
  </verify>
  <done>output/surrogate/best/ contains model.pt, normalizer.pt, config.json, selection.json; STATE.md Decisions section records the selected run and val_loss; Phase 3 is complete and Phase 4 can proceed</done>
</task>

</tasks>

<verification>
Phase 3 gate check — all must pass before Phase 4:

```bash
# Checkpoint files exist
ls output/surrogate/best/{model.pt,normalizer.pt,config.json,selection.json}

# Selection documented
python3 -c "import json; d=json.load(open('output/surrogate/best/selection.json')); print(d)"

# Experiment results written
head -5 experiments/surrogate-architecture-experiments-results.md

# All unit tests still green
python3 -m pytest tests/test_surrogate_arch.py -v
```
</verification>

<success_criteria>
- output/surrogate/best/ contains the confirmed best checkpoint with selection.json
- experiments/surrogate-architecture-experiments-results.md documents full experiment results
- STATE.md Decisions section records the Phase 3 architecture selection
- Human has reviewed and approved via the checkpoint task
- python3 -m pytest tests/test_surrogate_arch.py -v → all 4 tests GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/03-train-surrogate-model-using-supervised-learning/03-05-SUMMARY.md`
</output>
