---
phase: 03.1-surrogate-model-architecture-experiments-rollout-loss-residual-history-window
plan: "02"
subsystem: surrogate
tags: [surrogate, arch-sweep, rollout-loss, residual-mlp, sweep-runner, tdd, tmux]

# Dependency graph
requires:
  - phase: 03.1-01
    provides: ResidualSurrogateModel, HistorySurrogateModel, --rollout-weight/--rollout-steps CLI args in train_surrogate.py
  - phase: 03-train-surrogate-model-using-supervised-learning
    provides: sweep.py pattern, SurrogateTrainConfig, baseline val_loss=0.2161 at output/surrogate/sweep_lr1e3_h512x3/

provides:
  - aprx_model_elastica/arch_sweep.py — architecture experiment sweep runner with --dry-run ARCH-05 smoke test
  - ARCH_SWEEP_CONFIGS (5 entries: A1, A3, A4, A5, B1) exported for import
  - run_arch_sweep() function launching 5 sequential training jobs via subprocess
  - tmux gsd-train session running Experiments A+B (~3.75 hrs total)
  - output/surrogate/arch_sweep.log (live training log)

affects:
  - 03.1-03 (results analysis, arch_sweep_summary.json reading)
  - 04-validate-surrogate-model (best checkpoint from best_run in summary)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "arch_sweep.py follows sweep.py pattern: ARCH_SWEEP_CONFIGS list, subprocess.run() per config, metrics.json read, summary JSON save"
    - "Baseline injection: A2 (known result) read from metrics.json at BASELINE_CHECKPOINT, not re-trained"
    - "--dry-run exits before subprocess calls — ARCH-05 smoke test path"
    - "Intermediate summary save after each run for fault tolerance"

key-files:
  created:
    - aprx_model_elastica/arch_sweep.py
    - tests/test_arch_sweep.py
  modified: []

key-decisions:
  - "ARCH_SWEEP_CONFIGS has exactly 5 entries (A1/A3/A4/A5/B1) — A2 is injected from Phase 3 checkpoint, not re-trained"
  - "Default output-base is output/surrogate/arch_sweep (NOT output/surrogate) — isolates arch sweep from Phase 3 runs"
  - "Sweep launched in tmux gsd-train (already existed from Phase 3); command redirected to arch_sweep.log via tee"
  - "Intermediate arch_sweep_summary.json written after each run for crash recovery"

patterns-established:
  - "Arch sweep runner: ARCH_SWEEP_CONFIGS + baseline injection + --dry-run + per-run summary save"

requirements-completed: [ARCH-05]

# Metrics
duration: 5min
completed: "2026-03-10"
---

# Phase 3.1 Plan 02: Architecture Sweep Runner Summary

**arch_sweep.py sweep runner created (TDD, 11 tests GREEN) and launched in tmux gsd-train — 5 sequential architecture experiments running (~3.75 hrs, V100)**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-10T13:01:30Z
- **Completed:** 2026-03-10T13:06:30Z
- **Tasks:** 2
- **Files modified:** 2 (1 new source, 1 new test)

## Accomplishments

- Created `aprx_model_elastica/arch_sweep.py` following `sweep.py` pattern: `ARCH_SWEEP_CONFIGS` (5 entries), subprocess-based training launch, `metrics.json` read, `arch_sweep_summary.json` save
- Baseline A2 injected from `output/surrogate/sweep_lr1e3_h512x3/metrics.json` (`val_loss=0.2161`) — not re-trained
- `--dry-run` flag: prints config table with all 5 new runs + baseline and exits 0 (ARCH-05 smoke test)
- 11 tests added in `tests/test_arch_sweep.py` — all pass GREEN (TDD RED first, then GREEN)
- Arch sweep launched in `tmux gsd-train` session; epoch 1/200 confirmed running for `arch_A1_rw0.0`; `output/surrogate/arch_sweep.log` growing

## Task Commits

Each task was committed atomically:

1. **Task 1: RED test scaffold** - `fe9a22c` (test)
2. **Task 1: arch_sweep.py implementation (GREEN)** - `48ccdf9` (feat)
3. **Task 2: tmux launch confirmation** - `b3c3b44` (chore)

## Files Created/Modified

- `aprx_model_elastica/arch_sweep.py` — Sweep runner: ARCH_SWEEP_CONFIGS (5 entries), baseline injection, --dry-run, subprocess per config, intermediate summary saves
- `tests/test_arch_sweep.py` — 11 tests: config structure (5 entries, keys, names), B1 residual, A1 rw=0, A5 steps=16, --dry-run exits 0, prints table, no subprocess, --help args

## Decisions Made

- `ARCH_SWEEP_CONFIGS` has exactly 5 entries (A1, A3, A4, A5, B1); A2 is injected as baseline from Phase 3 `metrics.json` — avoids wasting ~45 min re-training an identical config
- Default `--output-base` is `output/surrogate/arch_sweep` (NOT `output/surrogate`) — keeps Phase 3 and Phase 3.1 sweep outputs separate
- `arch_sweep_summary.json` written incrementally after each run — if a run crashes, previous results are preserved
- Sweep launched in existing `tmux gsd-train` session using `tee` to write to `arch_sweep.log`

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `arch_sweep.py --dry-run` passes (ARCH-05 confirmed)
- 5 training jobs running sequentially in `tmux gsd-train` (~3.75 hrs remaining)
- Monitor: `tmux attach -t gsd-train` OR `tail -f output/surrogate/arch_sweep.log`
- After completion: `output/surrogate/arch_sweep/arch_sweep_summary.json` will have 5+1 result entries ranked by `val_loss`
- Plan 03.1-03 (results analysis + plot generation) should wait for sweep completion

---
*Phase: 03.1-surrogate-model-architecture-experiments-rollout-loss-residual-history-window*
*Completed: 2026-03-10*
