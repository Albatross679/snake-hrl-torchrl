---
phase: 06.1-improve-report-structure-and-organization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - report/report.tex
autonomous: true
requirements: []

must_haves:
  truths:
    - "Related Work appears before the physics backend sections, providing context for the reader"
    - "Physics backends (Elastica and DisMech) are unified under a single parent section with shared formulation"
    - "CPG control is described once in a shared subsection, not duplicated across backends"
    - "The force/torque comparison table appears once in the unified comparison subsection, not duplicated"
    - "RL Training sections (direct and surrogate) are merged into a single section"
  artifacts:
    - path: "report/report.tex"
      provides: "Restructured LaTeX report with improved section hierarchy"
      contains: "\\section{Related Work}"
  key_links:
    - from: "report/report.tex section ordering"
      to: "reader flow"
      via: "Introduction -> Related Work -> Physics -> Data -> Surrogate -> RL -> Discussion -> Conclusion"
      pattern: "section.Related Work.*section.Physics Simulation.*section.Data Collection.*section.Neural Surrogate.*section.Reinforcement Learning.*section.Discussion.*section.Conclusion"
---

<objective>
Restructure report/report.tex to improve the logical flow and reduce redundancy in the section hierarchy.

Purpose: The current report front-loads two extremely detailed physics backend sections (Elastica ~420 lines, DisMech ~280 lines) before Related Work, duplicates CPG control descriptions across both backends, and separates RL training into two thin sections. Restructuring will improve readability and reduce redundancy.

Output: A restructured report.tex with a cleaner section hierarchy, all content preserved, and improved logical flow.
</objective>

<execution_context>
@/home/user/snake-hrl-torchrl/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/snake-hrl-torchrl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@report/report.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure report.tex section hierarchy</name>
  <files>report/report.tex</files>
  <read_first>report/report.tex</read_first>
  <action>
Reorganize report.tex sections. **Move/merge only — no content rewriting.** All text preserved.

**Target section hierarchy:**

| # | Section | Action | Source lines |
|---|---------|--------|-------------|
| 1 | `\section{Introduction}` | KEEP | 166-176 |
| 2 | `\section{Related Work}` | MOVE UP (from after physics to after intro) | 984-1100 |
| 3 | `\section{Physics Simulation Backends}` | NEW parent (replaces separate Elastica/DisMech sections) | — |
| 4 | `\section{Data Collection}` | KEEP | 1103-1157 |
| 5 | `\section{Neural Surrogate Model}` | KEEP | 1160-1471 |
| 6 | `\section{Reinforcement Learning}` | MERGE two RL sections into one | 1474-1542 |
| 7 | `\section{Discussion}` | KEEP | 1545-1686 |
| 8 | `\section{Conclusion}` | KEEP | 1689-1693 |
| A | Appendix: Issue Tracker | KEEP | 1700-1911 |

**Physics subsection structure (section 3):**

| Subsection | Content source | Lines |
|------------|---------------|-------|
| `\subsection{Cosserat Rod Formulation}` | Shared formulation from sec:elastica:formulation (well-posedness tuple, PDEs, ODE semi-discrete, state vector table) | 184-292 |
| `\subsection{CPG Control}` | MERGE sec:elastica:cpg + sec:dismech:cpg into ONE. Keep Elastica version's action table/serpenoid eq. Add paragraph: "DisMech applies curvature through natural strain of discrete bend springs rather than the rest curvature array, and sets it once per RL step rather than per substep." | 595-627, 912-927 |
| `\subsection{PyElastica Backend}` | Algorithm, Staggered Grid, External Forces, "Explicit evaluation" paragraph | 296-551 |
| `\subsection{DisMech Backend}` | Formulation diffs (solver params table, primary unknowns, bending energy, state extraction), Algorithm, DER Discretization, Implicit Integration | 646-908 |
| `\subsection{Backend Comparison}` | MERGE sec:comparison + sec:elastica:force-comparison (both comparison tables under one subsection) | 554-591, 929-981 |

**RL subsection structure (section 6):**

| Subsection | Content source |
|------------|---------------|
| `\subsection{Direct Simulation Training}` | sec:rl-direct |
| `\subsection{Surrogate Environment Training}` | sec:rl-surrogate |
| `\subsection{Challenges}` | sec:rl-direct:challenges |

Parent label: `\label{sec:rl}`

**Label mapping (old -> new):**

| Old label | New label |
|-----------|-----------|
| `sec:elastica` | `sec:physics` |
| `sec:elastica:formulation` | `sec:physics:formulation` |
| `sec:elastica:cpg` | `sec:physics:cpg` |
| `sec:elastica:algorithm` | `sec:physics:elastica` |
| `sec:elastica:grid` | `sec:physics:grid` |
| `sec:elastica:forces` | `sec:physics:forces` |
| `sec:elastica:force-comparison` | REMOVED (merged into `sec:physics:comparison`) |
| `sec:dismech` | `sec:physics:dismech` |
| `sec:dismech:formulation` | `sec:physics:dismech:formulation` |
| `sec:dismech:algorithm` | `sec:physics:dismech:algorithm` |
| `sec:dismech:der` | `sec:physics:dismech:der` |
| `sec:dismech:implicit` | `sec:physics:dismech:implicit` |
| `sec:dismech:cpg` | REMOVED (merged into `sec:physics:cpg`) |
| `sec:comparison` | `sec:physics:comparison` |
| `sec:rl-direct` | `sec:rl:direct` |
| `sec:rl-direct:challenges` | `sec:rl:challenges` |
| `sec:rl-surrogate` | `sec:rl:surrogate` |
| `sec:rl-surrogate:results` | `sec:rl:surrogate:results` |

Update ALL `\cref{}` and `\label{}` references using old labels throughout the entire file.
  </action>
  <verify>
    <automated>cd /home/user/snake-hrl-torchrl && python3 -c "
import re
content = open('report/report.tex').read()
rw = content.find(r'\section{Related Work}')
phys = content.find(r'\section{Physics Simulation Backends}')
assert rw != -1, 'Related Work section missing'
assert phys != -1, 'Physics Simulation Backends section missing'
assert rw < phys, f'Related Work must precede Physics (got rw={rw}, phys={phys})'
assert r'\section{Solver Backend: Elastica}' not in content, 'Old Elastica section still present'
assert r'\section{DisMech Backend: Discrete Elastic Rods}' not in content, 'Old DisMech section still present'
assert r'\section{General Comparison Between Elastica and DisMech}' not in content, 'Old comparison section still present'
assert r'\section{RL Training with Elastica and DisMech}' not in content, 'Old RL direct section still present'
assert r'\section{RL Training with Trained Surrogate}' not in content, 'Old RL surrogate section still present'
assert content.count(r'\subsection{CPG Control}') == 1, 'CPG subsection should appear exactly once'
rl = content.find(r'\section{Reinforcement Learning}')
assert rl != -1, 'Merged RL section missing'
assert phys < rl, 'Physics must precede RL'
print('OK: section structure verified')
"</automated>
  </verify>
  <acceptance_criteria>
    - report/report.tex contains `\section{Related Work}` appearing BEFORE `\section{Physics Simulation Backends}`
    - report/report.tex contains `\section{Physics Simulation Backends}` as a single parent section
    - report/report.tex does NOT contain `\section{Solver Backend: Elastica}` (old section name removed)
    - report/report.tex does NOT contain `\section{DisMech Backend: Discrete Elastic Rods}` (old section name removed)
    - report/report.tex does NOT contain `\section{General Comparison Between Elastica and DisMech}` (merged into subsection)
    - report/report.tex does NOT contain `\section{RL Training with Elastica and DisMech}` (merged)
    - report/report.tex does NOT contain `\section{RL Training with Trained Surrogate}` (merged)
    - report/report.tex contains `\section{Reinforcement Learning}` as the merged RL section
    - report/report.tex contains `\subsection{CPG Control}` exactly once (not duplicated)
    - report/report.tex contains `\label{sec:physics}` for the physics parent section
    - report/report.tex contains `\label{sec:rl}` for the merged RL section
    - grep for old labels `sec:elastica}` returns 0 matches (all updated)
    - grep for `sec:dismech:cpg` returns 0 matches (merged into sec:physics:cpg)
    - grep for `sec:rl-direct}` returns 0 matches (updated to sec:rl:direct)
  </acceptance_criteria>
  <done>Report sections are reorganized with Related Work before physics, unified physics parent section, merged CPG, merged RL, and all labels updated.</done>
</task>

<task type="auto">
  <name>Task 2: Verify cross-references and fix any broken \cref{} links</name>
  <files>report/report.tex</files>
  <read_first>report/report.tex</read_first>
  <action>
Comprehensive cross-reference audit after Task 1 restructure.

**Steps:**
1. Extract all `\label{...}` definitions
2. Extract all `\cref{...}` and `\Cref{...}` references
3. Find orphaned refs (referenced but no matching label) and fix them

**Known breakage to fix in Introduction (lines 174-175):**

| Current text | Replace with |
|-------------|-------------|
| `\cref{sec:elastica,sec:dismech,sec:comparison}` | `\cref{sec:physics}` |
| `\cref{sec:rl-surrogate}` | `\cref{sec:rl}` |

**Likely broken refs to check:**

| Old ref | New ref |
|---------|---------|
| `\cref{sec:elastica}` | `\cref{sec:physics:elastica}` or `\cref{sec:physics}` (context-dependent) |
| `\cref{sec:dismech}` | `\cref{sec:physics:dismech}` |
| `\cref{sec:comparison}` | `\cref{sec:physics:comparison}` |
| `\cref{sec:elastica:cpg}` | `\cref{sec:physics:cpg}` |
| `\cref{sec:dismech:cpg}` | `\cref{sec:physics:cpg}` |

Verify `\lhead` header text is still appropriate (currently "Neural Surrogate for Snake Robot RL" — fine).
  </action>
  <verify>
    <automated>cd /home/user/snake-hrl-torchrl && python3 -c "
import re
content = open('report/report.tex').read()
labels = set(re.findall(r'\\\\label\{([^}]+)\}', content))
refs = set()
for m in re.findall(r'\\\\[Cc]ref\{([^}]+)\}', content):
    for r in m.split(','):
        refs.add(r.strip())
broken = refs - labels
if broken:
    print(f'BROKEN REFS: {broken}')
    exit(1)
else:
    print(f'OK: {len(refs)} refs, {len(labels)} labels, 0 broken')
"</automated>
  </verify>
  <acceptance_criteria>
    - The verification script prints "OK:" with 0 broken references
    - report/report.tex does not contain `\cref{sec:elastica}` (old label)
    - report/report.tex does not contain `\cref{sec:dismech}` without `:` suffix (old top-level label)
    - report/report.tex does not contain `\cref{sec:rl-direct}` (old label)
    - report/report.tex does not contain `\cref{sec:rl-surrogate}` (old label)
    - report/report.tex does not contain `\cref{sec:comparison}` (old label)
    - Introduction references updated to use new section labels
  </acceptance_criteria>
  <done>All cross-references in report.tex are valid -- every \cref{} points to an existing \label{}, and the Introduction paragraph reflects the new structure.</done>
</task>

</tasks>

<verification>
1. `grep -c '\\section{' report/report.tex` shows the expected number of \section commands (8 numbered + 1 unnumbered Notation + appendix sections)
2. The python cross-reference checker returns 0 broken references
3. Section ordering in report.tex follows: Introduction -> Related Work -> Physics Simulation Backends -> Data Collection -> Neural Surrogate Model -> Reinforcement Learning -> Discussion -> Conclusion
</verification>

<success_criteria>
- Report sections follow a logical reader flow: context (intro, related work) -> physics foundations -> data -> model -> RL -> discussion -> conclusion
- No content is deleted -- all existing text is preserved in the restructured layout
- CPG control is described once, not duplicated across two backend sections
- Force/torque comparison appears once in the comparison subsection
- RL training is a single section with direct and surrogate as subsections
- All \cref{} cross-references resolve to valid \label{} targets
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-improve-report-structure-and-organization/06.1-01-SUMMARY.md`
</output>
