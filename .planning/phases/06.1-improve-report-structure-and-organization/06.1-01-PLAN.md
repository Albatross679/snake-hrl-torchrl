---
phase: 06.1-improve-report-structure-and-organization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - report/report.tex
autonomous: true
requirements:
  - RSTR-01
  - RSTR-02
  - RSTR-03

must_haves:
  truths:
    - "Related Work appears before the physics backend sections, providing context for the reader"
    - "Physics backends (Elastica and DisMech) are unified under a single parent section with shared formulation"
    - "CPG control is described once in a shared subsection, not duplicated across backends"
    - "The force/torque comparison table appears once in the unified comparison subsection, not duplicated"
    - "RL Training sections (direct and surrogate) are merged into a single section"
  artifacts:
    - path: "report/report.tex"
      provides: "Restructured LaTeX report with improved section hierarchy"
      contains: "\\section{Related Work}"
  key_links:
    - from: "report/report.tex section ordering"
      to: "reader flow"
      via: "Introduction -> Related Work -> Physics -> Data -> Surrogate -> RL -> Discussion -> Conclusion"
      pattern: "section.Related Work.*section.Physics Simulation.*section.Data Collection.*section.Neural Surrogate.*section.Reinforcement Learning.*section.Discussion.*section.Conclusion"
---

<objective>
Restructure report/report.tex to improve the logical flow and reduce redundancy in the section hierarchy.

Purpose: The current report front-loads two extremely detailed physics backend sections (Elastica ~420 lines, DisMech ~280 lines) before Related Work, duplicates CPG control descriptions across both backends, and separates RL training into two thin sections. Restructuring will improve readability and reduce redundancy.

Output: A restructured report.tex with a cleaner section hierarchy, all content preserved, and improved logical flow.
</objective>

<execution_context>
@/home/user/snake-hrl-torchrl/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/snake-hrl-torchrl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@report/report.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure report.tex section hierarchy</name>
  <files>report/report.tex</files>
  <read_first>report/report.tex</read_first>
  <action>
Reorganize report.tex to the following section structure. All existing content is PRESERVED -- this is a move/merge operation, not a rewrite. The specific changes are:

**New section ordering (with old location in parentheses):**

1. `\section{Introduction}` -- KEEP AS-IS (currently lines 166-176)

2. `\section{Related Work}` -- MOVE UP from current position (lines 984-1100) to immediately after Introduction. This gives readers context on neural surrogates and RL for soft robots before the detailed physics sections. Keep both subsections (Neural Surrogates for ODE/PDE, RL Control for Soft Robots) unchanged.

3. `\section{Physics Simulation Backends}` -- NEW parent section replacing the current separate sections 2 and 3. Structure:
   - `\subsection{Cosserat Rod Formulation}` -- Contains the shared system formulation currently in sec:elastica:formulation (lines 184-292). This is shared between both backends, so present it once. Keep the well-posedness tuple table, PDE equations, ODE semi-discrete equation, equation count paragraph, integrator paragraph, and state vector table.
   - `\subsection{CPG Control}` -- MERGE the two CPG subsections (sec:elastica:cpg lines 595-627 and sec:dismech:cpg lines 912-927) into ONE subsection. Keep the action vector table, the serpenoid equation, and the action parameters description from sec:elastica:cpg. Add a paragraph noting the DisMech difference: "DisMech applies curvature through natural strain of discrete bend springs rather than the rest curvature array, and sets it once per RL step rather than per substep."
   - `\subsection{PyElastica Backend}` -- Contains: Algorithm subsection (sec:elastica:algorithm lines 296-438), Staggered Grid (sec:elastica:grid lines 442-501), External Force Handling (sec:elastica:forces lines 505-551), and the "Explicit evaluation, not equation solving" paragraph (lines 430-438).
   - `\subsection{DisMech Backend}` -- Contains: System Formulation differences from shared (sec:dismech:formulation solver params table lines 646-664, primary unknowns paragraph, discrete bending energy, state extraction table), Algorithm (sec:dismech:algorithm lines 715-818), DER Discretization (sec:dismech:der lines 822-848), Implicit Time Integration (sec:dismech:implicit lines 852-908).
   - `\subsection{Backend Comparison}` -- Contains the comprehensive comparison table (sec:comparison lines 929-981) AND the force/torque comparison table (sec:elastica:force-comparison lines 554-591). Remove the separate `\section{General Comparison}` and `\subsection{Internal and External Force Comparison}`. Combine both tables under this one subsection.

4. `\section{Data Collection}` -- KEEP AS-IS (lines 1103-1157)

5. `\section{Neural Surrogate Model}` -- KEEP AS-IS (lines 1160-1471)

6. `\section{Reinforcement Learning}` -- MERGE sections "RL Training with Elastica and DisMech" (lines 1474-1510) and "RL Training with Trained Surrogate" (lines 1513-1542) into ONE section with two subsections:
   - `\subsection{Direct Simulation Training}` (content from sec:rl-direct)
   - `\subsection{Surrogate Environment Training}` (content from sec:rl-surrogate)
   - `\subsection{Challenges}` (content from sec:rl-direct:challenges)
   Update the parent section label to `\label{sec:rl}`.

7. `\section{Discussion}` -- KEEP AS-IS (lines 1545-1686)

8. `\section{Conclusion}` -- KEEP AS-IS (lines 1689-1693)

9. Appendix: Issue Tracker -- KEEP AS-IS (lines 1700-1911)

**Label updates required with the restructure:**
- `sec:elastica` -> `sec:physics` (parent section)
- `sec:elastica:formulation` -> `sec:physics:formulation`
- `sec:elastica:cpg` -> `sec:physics:cpg`
- `sec:elastica:algorithm` -> `sec:physics:elastica`
- `sec:elastica:grid` -> `sec:physics:grid`
- `sec:elastica:forces` -> `sec:physics:forces`
- `sec:elastica:force-comparison` -> removed (merged into sec:physics:comparison)
- `sec:dismech` -> `sec:physics:dismech`
- `sec:dismech:formulation` -> `sec:physics:dismech:formulation`
- `sec:dismech:algorithm` -> `sec:physics:dismech:algorithm`
- `sec:dismech:der` -> `sec:physics:dismech:der`
- `sec:dismech:implicit` -> `sec:physics:dismech:implicit`
- `sec:dismech:cpg` -> removed (merged into sec:physics:cpg)
- `sec:comparison` -> `sec:physics:comparison`
- `sec:rl-direct` -> `sec:rl:direct`
- `sec:rl-direct:challenges` -> `sec:rl:challenges`
- `sec:rl-surrogate` -> `sec:rl:surrogate`
- `sec:rl-surrogate:results` -> `sec:rl:surrogate:results`

**All \cref{} and \label{} references using old labels MUST be updated to the new labels throughout the entire file.** Search for every occurrence of the old label strings and replace them.

**Do NOT modify any content within sections** -- only move blocks, merge duplicated CPG content, update section/subsection hierarchy levels, and update labels/references.
  </action>
  <verify>
    <automated>grep -c '\\\\section{' report/report.tex | head -1</automated>
  </verify>
  <acceptance_criteria>
    - report/report.tex contains `\section{Related Work}` appearing BEFORE `\section{Physics Simulation Backends}`
    - report/report.tex contains `\section{Physics Simulation Backends}` as a single parent section
    - report/report.tex does NOT contain `\section{Solver Backend: Elastica}` (old section name removed)
    - report/report.tex does NOT contain `\section{DisMech Backend: Discrete Elastic Rods}` (old section name removed)
    - report/report.tex does NOT contain `\section{General Comparison Between Elastica and DisMech}` (merged into subsection)
    - report/report.tex does NOT contain `\section{RL Training with Elastica and DisMech}` (merged)
    - report/report.tex does NOT contain `\section{RL Training with Trained Surrogate}` (merged)
    - report/report.tex contains `\section{Reinforcement Learning}` as the merged RL section
    - report/report.tex contains `\subsection{CPG Control}` exactly once (not duplicated)
    - report/report.tex contains `\label{sec:physics}` for the physics parent section
    - report/report.tex contains `\label{sec:rl}` for the merged RL section
    - grep for old labels `sec:elastica}` returns 0 matches (all updated)
    - grep for `sec:dismech:cpg` returns 0 matches (merged into sec:physics:cpg)
    - grep for `sec:rl-direct}` returns 0 matches (updated to sec:rl:direct)
  </acceptance_criteria>
  <done>Report sections are reorganized with Related Work before physics, unified physics parent section, merged CPG, merged RL, and all labels updated.</done>
</task>

<task type="auto">
  <name>Task 2: Verify cross-references and fix any broken \cref{} links</name>
  <files>report/report.tex</files>
  <read_first>report/report.tex</read_first>
  <action>
After the restructure in Task 1, do a comprehensive cross-reference audit:

1. Extract all `\label{...}` definitions in the file
2. Extract all `\cref{...}` and `\Cref{...}` references in the file
3. Check that every referenced label has a corresponding `\label{}` definition
4. Fix any broken references by updating to the correct new label

Also verify these specific references that are likely to break:
- `\cref{sec:elastica}` in Introduction line 175 -> should become `\cref{sec:physics:elastica}` or `\cref{sec:physics}` depending on context
- `\cref{sec:dismech}` in Introduction line 175 -> should become `\cref{sec:physics:dismech}`
- `\cref{sec:comparison}` in Introduction line 175 -> should become `\cref{sec:physics:comparison}`
- `\cref{sec:rl-surrogate}` in Introduction line 174 -> should become `\cref{sec:rl:surrogate}`
- Any `\cref{sec:elastica:cpg}` references -> should become `\cref{sec:physics:cpg}`
- Any `\cref{sec:dismech:cpg}` references -> should become `\cref{sec:physics:cpg}`

Also update the Introduction paragraph (line 174-175) to reflect the new section structure. Currently it says:
"Two physics backends are investigated --- PyElastica (explicit symplectic) and DisMech (implicit backward Euler) --- providing complementary perspectives on surrogate fidelity (\cref{sec:elastica,sec:dismech,sec:comparison})."
This should become:
"Two physics backends are investigated --- PyElastica (explicit symplectic) and DisMech (implicit backward Euler) --- providing complementary perspectives on surrogate fidelity (\cref{sec:physics})."

And the pipeline description in line 174:
"(3)~PPO training in the surrogate environment with a direct Elastica baseline for comparison (\cref{sec:rl-surrogate})"
should become:
"(3)~PPO training in the surrogate environment with a direct Elastica baseline for comparison (\cref{sec:rl})"

Finally, verify that the `\lhead` header text is still appropriate. It currently says "Neural Surrogate for Snake Robot RL" which is fine.
  </action>
  <verify>
    <automated>cd /home/user/snake-hrl-torchrl && python3 -c "
import re
content = open('report/report.tex').read()
labels = set(re.findall(r'\\\\label\{([^}]+)\}', content))
refs = set()
for m in re.findall(r'\\\\[Cc]ref\{([^}]+)\}', content):
    for r in m.split(','):
        refs.add(r.strip())
broken = refs - labels
if broken:
    print(f'BROKEN REFS: {broken}')
    exit(1)
else:
    print(f'OK: {len(refs)} refs, {len(labels)} labels, 0 broken')
"</automated>
  </verify>
  <acceptance_criteria>
    - The verification script prints "OK:" with 0 broken references
    - report/report.tex does not contain `\cref{sec:elastica}` (old label)
    - report/report.tex does not contain `\cref{sec:dismech}` without `:` suffix (old top-level label)
    - report/report.tex does not contain `\cref{sec:rl-direct}` (old label)
    - report/report.tex does not contain `\cref{sec:rl-surrogate}` (old label)
    - report/report.tex does not contain `\cref{sec:comparison}` (old label)
    - Introduction references updated to use new section labels
  </acceptance_criteria>
  <done>All cross-references in report.tex are valid -- every \cref{} points to an existing \label{}, and the Introduction paragraph reflects the new structure.</done>
</task>

</tasks>

<verification>
1. `grep -c '\\section{' report/report.tex` shows the expected number of \section commands (8 numbered + 1 unnumbered Notation + appendix sections)
2. The python cross-reference checker returns 0 broken references
3. Section ordering in report.tex follows: Introduction -> Related Work -> Physics Simulation Backends -> Data Collection -> Neural Surrogate Model -> Reinforcement Learning -> Discussion -> Conclusion
</verification>

<success_criteria>
- Report sections follow a logical reader flow: context (intro, related work) -> physics foundations -> data -> model -> RL -> discussion -> conclusion
- No content is deleted -- all existing text is preserved in the restructured layout
- CPG control is described once, not duplicated across two backend sections
- Force/torque comparison appears once in the comparison subsection
- RL training is a single section with direct and surrogate as subsections
- All \cref{} cross-references resolve to valid \label{} targets
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-improve-report-structure-and-organization/06.1-01-SUMMARY.md`
</output>
