---
phase: quick-260316-s3f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - papers/aprx_model_dismech/__init__.py
  - papers/aprx_model_dismech/__main__.py
  - papers/aprx_model_dismech/state.py
  - papers/aprx_model_dismech/train_config.py
  - papers/aprx_model_dismech/collect_data.py
  - papers/aprx_model_dismech/collect_config.py
  - papers/aprx_model_dismech/env.py
  - papers/aprx_model_dismech/validate.py
  - papers/aprx_model_dismech/dataset.py
  - papers/aprx_model_dismech/model.py
  - papers/aprx_model_dismech/train_surrogate.py
  - papers/aprx_model_dismech/health.py
  - papers/aprx_model_dismech/monitor.py
  - papers/aprx_model_dismech/preprocess_relative.py
  - report/report.tex
autonomous: true
requirements: []

must_haves:
  truths:
    - "papers/aprx_model_dismech/ package exists with all surrogate modules adapted for DisMech"
    - "state.py packs DisMech 3D state into identical 124-dim 2D format (pos_x(21), pos_y(21), vel_x(21), vel_y(21), yaw(20), omega_z(20))"
    - "collect_data.py uses DisMech SnakeRobot from src/physics/snake_robot.py instead of LocomotionElasticaEnv"
    - "model.py, dataset.py, and train_surrogate.py are reused without architecture changes (physics-agnostic)"
    - "Chapter 3 (DisMech Backend) in report.tex replaces the placeholder with DER formulation, implicit integration, algorithm, and surrogate I/O table"
  artifacts:
    - path: "papers/aprx_model_dismech/state.py"
      provides: "DisMech state packing/unpacking to 124-dim format"
      contains: "pack_from_dismech"
    - path: "papers/aprx_model_dismech/collect_data.py"
      provides: "Data collection from DisMech env"
      contains: "SnakeRobot"
    - path: "papers/aprx_model_dismech/train_config.py"
      provides: "Config pointing to DisMech physics"
      contains: "SurrogateTrainConfig"
    - path: "report/report.tex"
      provides: "Chapter 3 written content"
      contains: "Discrete Elastic Rod Discretization"
  key_links:
    - from: "papers/aprx_model_dismech/state.py"
      to: "src/physics/snake_robot.py"
      via: "DisMech state.q and state.u extraction"
      pattern: "_dismech_robot\\.state\\.(q|u)"
    - from: "papers/aprx_model_dismech/collect_data.py"
      to: "src/physics/snake_robot.py"
      via: "import SnakeRobot"
      pattern: "from src\\.physics\\.snake_robot import"
---

<objective>
Create a DisMech surrogate model package (`papers/aprx_model_dismech/`) by adapting the existing Elastica surrogate (`papers/aprx_model_elastica/`), and write Chapter 3 (DisMech Backend: Discrete Elastic Rods) in `report/report.tex`.

Purpose: The Elastica surrogate already works. DisMech uses the same snake (21 nodes, 20 elements) but with implicit Euler integration and 3D state representation. The surrogate architecture is physics-agnostic if the state dimensionality matches, so most files can be reused directly. The report chapter formalizes the DER formulation and contrasts it with the Elastica chapter.

Output: Complete `papers/aprx_model_dismech/` package + written Chapter 3 in report.tex.
</objective>

<execution_context>
@/home/user/snake-hrl-torchrl/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/snake-hrl-torchrl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@papers/aprx_model_elastica/state.py
@papers/aprx_model_elastica/train_config.py
@papers/aprx_model_elastica/collect_data.py
@papers/aprx_model_elastica/env.py
@papers/aprx_model_elastica/validate.py
@papers/aprx_model_elastica/model.py
@papers/aprx_model_elastica/dataset.py
@papers/aprx_model_elastica/train_surrogate.py
@papers/aprx_model_elastica/collect_config.py
@papers/aprx_model_elastica/health.py
@papers/aprx_model_elastica/monitor.py
@papers/aprx_model_elastica/preprocess_relative.py
@papers/aprx_model_elastica/__init__.py
@papers/aprx_model_elastica/__main__.py
@src/physics/snake_robot.py
@src/configs/physics.py
@report/report.tex
@report/time-integration.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create papers/aprx_model_dismech/ package with DisMech state extraction</name>
  <files>
    papers/aprx_model_dismech/__init__.py
    papers/aprx_model_dismech/__main__.py
    papers/aprx_model_dismech/state.py
    papers/aprx_model_dismech/train_config.py
    papers/aprx_model_dismech/collect_data.py
    papers/aprx_model_dismech/collect_config.py
    papers/aprx_model_dismech/env.py
    papers/aprx_model_dismech/validate.py
    papers/aprx_model_dismech/dataset.py
    papers/aprx_model_dismech/model.py
    papers/aprx_model_dismech/train_surrogate.py
    papers/aprx_model_dismech/health.py
    papers/aprx_model_dismech/monitor.py
    papers/aprx_model_dismech/preprocess_relative.py
  </files>
  <action>
    Create the `papers/aprx_model_dismech/` directory. The key file is `state.py` -- all others either reuse Elastica code or need minor import changes.

    **state.py** (NEEDS FULL REWRITE for DisMech):
    - Keep all constants identical: NUM_NODES=21, NUM_ELEMENTS=20, RAW_STATE_DIM=124, REL_STATE_DIM=128, all slice definitions, ACTION_DIM=5, TIME_ENC_DIM=4, PER_ELEMENT_PHASE_DIM=60.
    - Keep `RodState2D` class but replace `pack_from_rod()` with `pack_from_dismech(snake_robot)`:
      - Extract positions: `q = snake_robot._dismech_robot.state.q; positions = q[:3*21].reshape(21, 3)`
      - pos_x = positions[:, 0] (x-coordinates), pos_y = positions[:, 1] (y-coordinates). Ignore z (2D mode).
      - Extract velocities: `u = snake_robot._dismech_robot.state.u; velocities = u[:3*21].reshape(21, 3)`
      - vel_x = velocities[:, 0], vel_y = velocities[:, 1].
      - Compute yaw (20 elements): tangent_e = positions[i+1] - positions[i]; yaw_e = arctan2(tangent_y, tangent_x) for each element.
      - Compute omega_z (20 elements): From velocity differences -- angular_velocity_e = (vel_tangential_cross / segment_length). Specifically: dx = positions[i+1] - positions[i], dv = velocities[i+1] - velocities[i]; omega_z_e = (dx[0]*dv[1] - dx[1]*dv[0]) / (dx[0]**2 + dx[1]**2 + 1e-10). This computes d(yaw)/dt from the cross product of tangent and velocity difference.
      - Return np.concatenate([pos_x, pos_y, vel_x, vel_y, yaw, omega_z]).astype(np.float32) -- same 124-dim layout.
    - Replace `pack_forces()` -- DisMech does not expose force arrays the same way. Make it return an empty dict or extract from `_dismech_robot.state` if available; otherwise stub with zeros and add a TODO comment.
    - Keep ALL other functions IDENTICAL: `unpack_positions_xy`, `unpack_velocities_xy`, `raw_to_relative`, `relative_to_raw`, all phase encoding functions, `StateNormalizer`. These are physics-agnostic and work on the 124/128-dim arrays regardless of backend.

    **Files to COPY UNCHANGED from aprx_model_elastica/ (just symlink or copy):**
    - `model.py` -- Architecture is physics-agnostic (input dim = 128+5+60=193, output dim = 128).
    - `dataset.py` -- Works on .pt files with states/next_states/actions/t_start tensors.
    - `train_surrogate.py` -- Training loop, loss functions, scheduling are all physics-agnostic.
    - `health.py` -- Generic health monitoring.
    - `monitor.py` -- Generic progress monitoring.
    - `preprocess_relative.py` -- Converts 124->128 dim using raw_to_relative (physics-agnostic).

    **Files needing MINOR ADAPTATION:**
    - `__init__.py` -- Same structure as Elastica, update docstring to say "DisMech" instead of "PyElastica".
    - `__main__.py` -- Update import path from aprx_model_elastica to aprx_model_dismech.
    - `train_config.py` -- Change imports to point to DisMech config classes instead of locomotion_elastica.config. Import PhysicsConfig from src/configs/physics. Change default data_dir to "data/surrogate_dismech_rl_step" and "data/surrogate_dismech_rl_step_rel128". Change wandb_project to "snake-hrl-surrogate-dismech". The SurrogateModelConfig, SurrogateTrainConfig structure stays the same. For SurrogateEnvConfig, replace LocomotionElasticaPhysicsConfig/SerpenoidControlConfig with DisMech-compatible equivalents from src/configs/physics.py.
    - `collect_data.py` -- This is the SECOND most complex adaptation. Replace LocomotionElasticaEnv with a simpler collection loop that:
      (a) Instantiates SnakeRobot from src/physics/snake_robot.py with PhysicsConfig.
      (b) Generates random CPG actions (same Sobol quasi-random or uniform random).
      (c) Applies curvature via snake_robot.set_curvature_control() using the serpenoid formula.
      (d) Calls snake_robot.step() to advance one implicit step (dt=0.05s).
      (e) Packs state before and after using pack_from_dismech().
      (f) Saves batches in same .pt format: {states, next_states, actions, t_start, episode_ids, step_ids}.
      The multiprocessing, health monitoring, and W&B infrastructure can be copied from the Elastica version with the env creation replaced.
    - `collect_config.py` -- Same structure but default num_transitions, data_dir, dt etc. adjusted for DisMech (dt=0.05s, steps_per_run=1).
    - `env.py` -- Replace references to LocomotionElasticaEnv with DisMech-based env. The SurrogateLocomotionEnv class itself is physics-agnostic (it loads a trained model and rolls forward), but the observation/reward computation references to the Elastica env config need updating.
    - `validate.py` -- Replace LocomotionElasticaEnv ground truth collection with DisMech SnakeRobot. The validation logic (multi-step rollout comparison) stays the same.

    **Key design decision:** DT_CTRL in state.py must change from 0.5s (Elastica: 500 substeps * 0.001s) to 0.05s (DisMech: 1 step * 0.05s). This affects n_cycles encoding. Add `DT_CTRL = 0.05` in the DisMech state.py.

    **Important:** For physics-agnostic files (model.py, dataset.py, train_surrogate.py, health.py, monitor.py, preprocess_relative.py), COPY them and update only the import paths from `aprx_model_elastica` to `aprx_model_dismech`. Do NOT create symlinks -- separate copies allow future divergence and avoid import confusion.
  </action>
  <verify>
    <automated>cd /home/user/snake-hrl-torchrl && python -c "from papers.aprx_model_dismech.state import RodState2D, pack_from_dismech, RAW_STATE_DIM, REL_STATE_DIM, raw_to_relative, relative_to_raw; print(f'RAW={RAW_STATE_DIM}, REL={REL_STATE_DIM}'); assert RAW_STATE_DIM == 124; assert REL_STATE_DIM == 128; print('OK')" 2>&1 | tail -5</automated>
  </verify>
  <done>
    - papers/aprx_model_dismech/ directory exists with all 14 Python files.
    - state.py has pack_from_dismech() that extracts (x,y) from 3D DisMech state, computes yaw from segment tangents, computes omega_z from velocity cross products.
    - DT_CTRL = 0.05 (DisMech single-step) instead of 0.5 (Elastica 500-substep).
    - All imports within the package reference aprx_model_dismech, not aprx_model_elastica.
    - RAW_STATE_DIM=124, REL_STATE_DIM=128 -- same as Elastica for model compatibility.
    - collect_data.py uses SnakeRobot from src/physics/snake_robot.py.
    - train_config.py points to DisMech config classes and data directories.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Chapter 3 (DisMech Backend) in report.tex</name>
  <files>report/report.tex</files>
  <action>
    Replace the \placeholder{...} block at line 554 in report.tex (inside \section{DisMech Backend: Discrete Elastic Rods}) with full content. Mirror the structure of Chapter 2 (Elastica section) but with DisMech-specific physics.

    **Structure to write:**

    1. **Opening paragraph** (2-3 sentences): Introduce DisMech as the second physics backend. Mention Discrete Elastic Rods (DER) by Bergou et al. 2008/2010. State the key difference: implicit Euler integration (unconditionally stable) vs Elastica's explicit symplectic integration.

    2. **\subsection{System Formulation}** (mirror sec:elastica:formulation):
       - Well-posedness tuple paragraph: same PDE (Cosserat rod), same domain, same BCs, same ICs, but different numerical method.
       - The governing PDEs are identical (\cref the equations from Chapter 2, do NOT repeat them). State: "The continuous Cosserat rod PDEs (\cref{eq:cosserat-linear,eq:cosserat-angular}) apply identically to the DisMech backend."
       - **Key difference paragraph:** DER uses vertex positions and a material frame (Bishop frame) as the primary unknowns, rather than the directors used in PyElastica. The discrete bending energy at vertex $i$ is $E_{\text{bend},i} = \frac{1}{2}(\boldsymbol{\kappa}_i - \boldsymbol{\kappa}_i^{\text{rest}})^\top \mathbf{B} (\boldsymbol{\kappa}_i - \boldsymbol{\kappa}_i^{\text{rest}}) / \bar{\ell}_i$ where $\bar{\ell}_i$ is the Voronoi length.
       - **State vector table** (same 124-dim format as Elastica): Identical to \cref{tab:state-vector}. Reference it: "The state vector uses the identical 124-dimensional layout shown in \cref{tab:state-vector}." Add a brief note that positions are extracted from the 3D state vector $\mathbf{q}$ by taking only (x,y) components, and yaw/omega_z are computed from finite differences rather than directly stored.

    3. **\subsection{Discrete Elastic Rod Discretization}** (mirror sec:elastica:grid):
       - DER discretization paragraph: Describe the vertex-edge-hinge structure. Vertices store positions (analogous to nodes in Elastica). Edges connect adjacent vertices. Hinges (analogous to Voronoi nodes) sit between edges and are where discrete curvature is computed.
       - **No staggered grid figure needed** -- reference the staggered grid figure from Chapter 2 and note the structural similarity: "The three-layer staggered grid (\cref{fig:staggered-grid}) applies to both backends; DisMech terms the middle layer \textit{edges} and the curvature layer \textit{hinges}."
       - **Action vector**: Same CPG parameters (\cref{tab:action-vector}).
       - **Control mechanism paragraph**: "DisMech applies curvature control through the natural strain of discrete bend springs. Setting \texttt{bend\_springs.nat\_strain[i, 0] = $\kappa_i^{\text{rest}}$} modifies the rest curvature that the elastic energy drives the rod toward --- the same physical mechanism as PyElastica's \texttt{rest\_kappa} array, but accessed through a different API."

    4. **\subsection{Implicit Time Integration}** (NEW -- no Elastica equivalent):
       - **Backward Euler formulation**: Present the update equations (from time-integration.tex):
         $\mathbf{v}_{n+1} = \mathbf{v}_n + \Delta t \, \mathbf{M}^{-1} \mathbf{F}(\mathbf{q}_{n+1}, \mathbf{v}_{n+1})$
         $\mathbf{q}_{n+1} = \mathbf{q}_n + \Delta t \, \mathbf{v}_{n+1}$
       - **Nonlinear residual**: $\mathbf{g}(\mathbf{q}_{n+1}) := \mathbf{M}(\mathbf{q}_{n+1} - 2\mathbf{q}_n + \mathbf{q}_{n-1}) - \Delta t^2 \mathbf{F}(\mathbf{q}_{n+1}) = \mathbf{0}$
       - **Newton solver paragraph**: Iterations $\mathbf{q}^{(k+1)} = \mathbf{q}^{(k)} - \mathbf{J}^{-1} \mathbf{g}(\mathbf{q}^{(k)})$ with sparse Jacobian $\mathbf{J}$ solved via PARDISO. Convergence criteria: $\|\mathbf{g}\| < \epsilon_f = 10^{-4}$ or $\|\delta\mathbf{q}\| < \epsilon_d = 10^{-2}$, max 25 iterations.
       - **Comparison table** (from time-integration.tex tab:comparison): Include the explicit vs implicit comparison table with properties: integration scheme, timestep per call, steps per second, stability, substep loop, cost per step, energy behavior, curvature control, solver parameters.

    5. **Algorithm block** (mirror Algorithm 1 from Chapter 2):
       - Create Algorithm (two-column format like Algorithm 1 in the Elastica section) for "DisMech Transition Operator $T_{\text{DER}}(\mathbf{s}_t, \mathbf{a}_t, t_{\text{step}}) \to \mathbf{s}_{t+1}$".
       - Left column: parameters ($\Delta t = 0.05$s, $\epsilon_f$, $\epsilon_d$, max\_iter=25, $\mathbf{B}$ stiffness, $\mu$ friction), state ($\mathbf{q} \in \mathbb{R}^{63}$ positions, $\mathbf{v} \in \mathbb{R}^{63}$ velocities -- noting 21 nodes x 3D but z=0), CPG params.
       - Right column: pseudocode:
         1. Set curvature: $\kappa_j^{\text{rest}} \gets A \sin(2\pi k s_j + 2\pi f t_{\text{step}} + \phi_0) + b$ for $j = 1,...,19$
         2. Initial guess: $\mathbf{q}^{(0)} \gets \mathbf{q} + \Delta t \mathbf{v}$
         3. Newton loop: assemble residual $\mathbf{g}$, Jacobian $\mathbf{J}$, solve $\delta\mathbf{q} = \mathbf{J}^{-1}\mathbf{g}$, update $\mathbf{q}^{(k+1)}$, check convergence
         4. Update velocity: $\mathbf{v}' \gets (\mathbf{q}^{(K)} - \mathbf{q}) / \Delta t$
         5. Return state
       - Cost line: "Cost: 1 Newton solve with $\sim$3--5 iterations, $\sim$12 ms on CPU (sparse PARDISO). No substep loop."

    6. **Surrogate approximation target** (mirror the Elastica version):
       - Same chain equation but with DisMech-specific notation:
         $\text{Cosserat PDE} \xrightarrow{\epsilon_{\text{space}}} \dot{\mathbf{s}} = \mathbf{g}_{\text{DER}}(\mathbf{s}, \mathbf{a}, t) \xrightarrow{\epsilon_{\text{time}}} T_{\text{DER}} = \text{Newton}(\Delta t) \xrightarrow{\epsilon_{\text{nn}}} f_\theta$
       - Same surrogate I/O table as \cref{tab:surrogate-io} but with DisMech costs. Reference the Elastica table: "The surrogate input/output structure is identical to \cref{tab:surrogate-io}; only the simulator cost changes from $\sim$4.6 ms (Elastica, 50 substeps) to $\sim$12 ms (DisMech, single Newton solve)."

    7. **Closing paragraph**: Emphasize that the surrogate architecture is backend-agnostic -- the same MLP that learns Elastica dynamics can be retrained on DisMech data without any change to the network, dataset format, or training procedure. The only adaptation is in the state extraction layer that packs DisMech's 3D vertex state into the shared 124-dimensional representation.

    **LaTeX conventions (from existing report.tex):**
    - Use \citet{} and \citep{} for citations (natbib).
    - Use \Cref{} at sentence start, \cref{} mid-sentence.
    - Use \textbf{} labels within paragraphs for emphasis (not nested subsubsections).
    - Algorithm blocks use algorithm/algpseudocode environments.
    - Tables use booktabs (toprule/midrule/bottomrule).
    - Add labels: sec:dismech:formulation, sec:dismech:der, sec:dismech:implicit, tab:dismech-comparison, alg:dismech-transition.
    - Add BibTeX entries for Bergou et al. 2008 and 2010 to references.bib if not already present.
  </action>
  <verify>
    <automated>cd /home/user/snake-hrl-torchrl && grep -c "\\\\subsection" report/report.tex | head -1 && grep -c "placeholder" report/report.tex && grep "Discrete Elastic Rod" report/report.tex | head -3</automated>
  </verify>
  <done>
    - The \placeholder{...} at line 554 is completely replaced with written content.
    - Chapter 3 has at least 3 subsections: System Formulation, Discrete Elastic Rod Discretization, Implicit Time Integration.
    - Contains a comparison table (explicit vs implicit), an algorithm block for DisMech transition, and surrogate I/O discussion.
    - All \cref cross-references to Chapter 2 tables/figures are valid.
    - Zero \placeholder or \mytodo markers in the DisMech section.
    - Bergou 2008 and 2010 cited.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document changes in logs/</name>
  <files>
    logs/dismech-surrogate-package-and-chapter3.md
  </files>
  <action>
    Create a log entry documenting both changes:
    1. Creation of papers/aprx_model_dismech/ package with all files listed.
    2. Chapter 3 content written in report.tex.

    Use required frontmatter: name, description, type: log, status: complete, subtype: feature, created, updated, tags, aliases.

    Include a brief summary of:
    - What state.py pack_from_dismech() does differently from pack_from_rod()
    - Which files were copied vs adapted
    - DT_CTRL change (0.5 -> 0.05)
    - Chapter 3 structure overview
  </action>
  <verify>
    <automated>test -f /home/user/snake-hrl-torchrl/logs/dismech-surrogate-package-and-chapter3.md && head -5 /home/user/snake-hrl-torchrl/logs/dismech-surrogate-package-and-chapter3.md</automated>
  </verify>
  <done>Log file exists with complete frontmatter and summary of both the code package and the report chapter.</done>
</task>

</tasks>

<verification>
1. `python -c "from papers.aprx_model_dismech.state import RodState2D, RAW_STATE_DIM; assert RAW_STATE_DIM == 124"` passes.
2. `python -c "from papers.aprx_model_dismech.model import SurrogateModel; print('OK')"` passes.
3. `python -c "from papers.aprx_model_dismech.dataset import FlatStepDataset; print('OK')"` passes.
4. `grep -c "placeholder" report/report.tex` returns 0 for the DisMech section (may still have placeholders in other sections).
5. `grep "Discrete Elastic Rod Discretization" report/report.tex` finds the subsection.
</verification>

<success_criteria>
- papers/aprx_model_dismech/ is a complete Python package with 14 files, importable without errors.
- state.py correctly extracts 2D state from DisMech's 3D representation into the shared 124-dim format.
- Chapter 3 in report.tex is fully written with DER formulation, implicit integration, algorithm, comparison table, and surrogate discussion -- no remaining placeholders.
- All cross-references to Chapter 2 content (equations, tables, figures) use \cref properly.
</success_criteria>

<output>
After completion, create `.planning/quick/260316-s3f-create-surrogate-model-for-dismech-based/260316-s3f-SUMMARY.md`
</output>
