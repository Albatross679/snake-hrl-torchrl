---
phase: quick
plan: 260317-l6p
type: execute
wave: 1
depends_on: []
files_modified:
  - report/report.tex
autonomous: true
requirements: []
must_haves:
  truths:
    - "Sections 2.2 and 2.3 are merged into a single section covering RL for snake and soft robots"
    - "One consolidated table replaces the two separate tables, with a Replicated column"
    - "No duplicated concluding remarks — simulation cost bottleneck discussed once"
    - "All existing citations and label references remain valid"
  artifacts:
    - path: "report/report.tex"
      provides: "Merged section 2.2 with consolidated table"
      contains: "\\subsection{RL Control for Snake and Soft Robots}"
  key_links:
    - from: "report/report.tex"
      to: "report/references.bib"
      via: "\\citet and \\citep references"
      pattern: "\\\\cite[tp]?\\{"
---

<objective>
Merge report sections 2.2 (RL Control for Soft Robots) and 2.3 (Snake Robot Locomotion and Navigation) into a single section "RL Control for Snake and Soft Robots" with one consolidated table. Eliminates duplication in both table content and concluding discussion paragraphs.

Purpose: Reduce redundancy — both sections cover RL for soft/snake robots with nearly identical table columns (Reference, Simulator, Action space, RL algo) and both conclude with the same simulation cost bottleneck theme.
Output: Updated report/report.tex with merged section and consolidated table.
</objective>

<execution_context>
@/home/user/snake-hrl-torchrl/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/snake-hrl-torchrl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@report/report.tex (lines 239-375: sections 2.2 and 2.3)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge sections 2.2 and 2.3 into a single section with consolidated table</name>
  <files>report/report.tex</files>
  <action>
Replace sections 2.2 (lines 239-294) and 2.3 (lines 296-375) with a single merged section. Specifically:

1. **Section heading:** Replace `\subsection{RL Control for Soft Robots}` with `\subsection{RL Control for Snake and Soft Robots}`. Keep label `\label{sec:related:rl}`. Remove `\label{sec:related:snake}` but add a comment `% formerly sec:related:snake` so any stale refs are traceable.

2. **Introductory paragraph:** Write 2-3 sentences introducing the consolidated table. Mention that the table covers both general soft-robot RL methods and snake-specific locomotion/navigation work. Note that several entries were reimplemented as baselines (indicated in the table).

3. **Consolidated table:** Create a single table `tab:related-rl-snake` with these columns:
   - Reference (p{2.0cm})
   - Robot type (p{1.8cm})
   - Action space (p{2.0cm})
   - RL algo. (p{1.2cm})
   - Simulator (p{1.5cm})
   - Replicated (p{0.8cm}) — checkmark or dash
   - Key finding (p{3.5cm})

   Entries in order (group by theme, not by original table):
   a. Snake/soft robot entries from old tab:related-snake (8 entries: Shi2020, Liu2021, Liu2022, Liu2023, Zheng2022, Jiang2024, Schaffer2024, Choi2025) — all get checkmark in Replicated column
   b. General soft robot entries from old tab:related-rl (Bing2019, Hong2026, MBPO/Janner2019, GNN/SanchezGonzalez2020) — get dash in Replicated column
   c. "This work" row at the bottom after \midrule

   For entries originally in tab:related-rl that lacked "Robot type", infer from context:
   - Bing2019: "Soft snake" (PyElastica snake)
   - Hong2026: "Soft robot" (FEM)
   - Janner2019: "Rigid articulated" (MuJoCo)
   - SanchezGonzalez2020: "Particle system" (not a robot per se)

   Use `\checkmark` for replicated column (add `\usepackage{amssymb}` if not already present, or use $\checkmark$ from standard math). Use `---` for non-replicated.

   Caption: "RL methods for snake and soft robot control. Entries marked with \checkmark were reimplemented as baselines."
   Label: `tab:related-rl-snake`

4. **Concluding discussion:** Merge the two concluding paragraphs into a single cohesive discussion (3-4 sentences). Themes to cover exactly once:
   - CPG-based action spaces dominate snake robot RL (cite Bing2019, Liu2021, Liu2023, Zheng2022, Jiang2024)
   - Curriculum learning and domain randomization bridge the sim-to-real gap (cite Liu2023, Zheng2022)
   - Simulation cost bottleneck is universal: MuJoCo ~1ms, Cosserat rod 12-46ms per step
   - Neural surrogate strategy as model-based RL interpretation — learned full transition operator, not latent dynamics

5. **Fix any \Cref/\autoref references:** Search entire file for `sec:related:snake` and `tab:related-snake` and `tab:related-rl`. Update to new labels (`sec:related:rl` stays, tables become `tab:related-rl-snake`). If `sec:related:snake` is referenced elsewhere, point those refs to `sec:related:rl` instead.

6. **Section 3 (Physics Simulation Backends) must immediately follow** the merged section — verify the `\section{Physics Simulation Backends}` heading and its `\label{sec:physics}` are preserved and appear right after the merged content.
  </action>
  <verify>
    <automated>cd /home/user/snake-hrl-torchrl && grep -c "\\\\subsection{RL Control for Snake and Soft Robots}" report/report.tex | grep -q "1" && grep -c "tab:related-rl-snake" report/report.tex | grep -q -E "[2-9]|[1-9][0-9]" && grep -c "\\\\subsection{Snake Robot Locomotion" report/report.tex | grep -q "0" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Old subsection 2.2 and 2.3 replaced by single "RL Control for Snake and Soft Robots" subsection
    - One consolidated table (tab:related-rl-snake) with 12 entries + "This work" row and a Replicated column
    - Old labels tab:related-rl, tab:related-snake removed; new label tab:related-rl-snake used
    - sec:related:snake label removed or redirected to sec:related:rl
    - Single concluding paragraph covers CPG dominance, curriculum learning, cost bottleneck, and surrogate-as-model-based-RL themes without duplication
    - Section 3 (Physics Simulation Backends) immediately follows
  </done>
</task>

</tasks>

<verification>
- `grep "\\subsection{Snake Robot Locomotion" report/report.tex` returns nothing (old section gone)
- `grep "tab:related-snake" report/report.tex` returns nothing (old table label gone)
- `grep "tab:related-rl-snake" report/report.tex` returns at least 2 hits (label + caption reference)
- `grep "\\checkmark" report/report.tex` returns 8 hits (one per replicated baseline)
- `grep "\\section{Physics Simulation Backends}" report/report.tex` still returns 1 hit (section 3 preserved)
</verification>

<success_criteria>
Sections 2.2 and 2.3 are replaced by a single section with one consolidated table. No content is lost — all 12 references are present. Duplication in concluding remarks is eliminated. All cross-references compile without errors.
</success_criteria>

<output>
After completion, create `.planning/quick/260317-l6p-restructure-report-sections-2-2-and-2-3-/260317-l6p-SUMMARY.md`
</output>
