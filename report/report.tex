% Neural Surrogate Modeling for Snake Robot Locomotion
% Research Report — Graduation Project Deliverable

\documentclass[11pt]{article}

% ---------------------------------------------------------------------------
% Font and encoding
% ---------------------------------------------------------------------------
\usepackage{palatino}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

% ---------------------------------------------------------------------------
% Page layout
% ---------------------------------------------------------------------------
\usepackage[top=1in, bottom=1in, left=1.25in, right=1.25in]{geometry}
\usepackage{setspace}
\setstretch{1.1}
\setlength{\parindent}{1em}
\setlength{\parskip}{0.4em}

% ---------------------------------------------------------------------------
% Math
% ---------------------------------------------------------------------------
\usepackage{amsmath}
\usepackage{amssymb}

% ---------------------------------------------------------------------------
% Figures and tables
% ---------------------------------------------------------------------------
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{float}
\usepackage{array}
\usepackage{booktabs}
\usepackage{multirow}
\graphicspath{{../figures/}}

% ---------------------------------------------------------------------------
% Colors
% ---------------------------------------------------------------------------
\usepackage{xcolor}

% ---------------------------------------------------------------------------
% Lists
% ---------------------------------------------------------------------------
\usepackage{enumitem}
\setlist[itemize]{nosep, topsep=0.3em, itemsep=0.2em}

% ---------------------------------------------------------------------------
% Algorithms
% ---------------------------------------------------------------------------
\usepackage{algorithm}
\usepackage{algpseudocode}

% ---------------------------------------------------------------------------
% Section spacing
% ---------------------------------------------------------------------------
\usepackage{titlesec}
\titlespacing*{\section}{0pt}{1.2em}{0.6em}
\titlespacing*{\subsection}{0pt}{0.9em}{0.3em}
\titlespacing*{\subsubsection}{0pt}{0.7em}{0.2em}

% ---------------------------------------------------------------------------
% Header and footer
% ---------------------------------------------------------------------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\lhead{\small Neural Surrogate for Snake Robot RL}
\rfoot{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

% ---------------------------------------------------------------------------
% Table of contents and section depth
% ---------------------------------------------------------------------------
\setcounter{tocdepth}{3}
\setcounter{secnumdepth}{3}

% ---------------------------------------------------------------------------
% Placeholder macros
% ---------------------------------------------------------------------------
\newcommand{\placeholder}[1]{\textcolor{gray}{\textit{[Placeholder: #1]}}}
\newcommand{\mytodo}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}

% ---------------------------------------------------------------------------
% Bibliography (natbib — before hyperref)
% ---------------------------------------------------------------------------
\usepackage[round, sort, authoryear]{natbib}

% ---------------------------------------------------------------------------
% Hyperref and microtype — before cleveref
% ---------------------------------------------------------------------------
\usepackage[hidelinks]{hyperref}
\usepackage{microtype}

% ---------------------------------------------------------------------------
% Cross-references (cleveref — MUST be loaded after hyperref)
% ---------------------------------------------------------------------------
\usepackage{cleveref}

% ---------------------------------------------------------------------------
% Title
% ---------------------------------------------------------------------------
\title{%
  \textbf{Neural Surrogate Modeling for Snake Robot Locomotion:}\\[0.3em]
  \large Faster Reinforcement Learning via Learned Cosserat Rod Dynamics%
}
\author{%
  Qifan Wen\\
  \small Electrical and Computer Engineering\\
  \small The Ohio State University\\
  \small \texttt{wen.679@buckeyemail.osu.edu}\\[0.6em]
  \small \textit{Advisor:} Prof.\ Abhishek Gupta\quad\small\texttt{gupta.706@osu.edu}%
}
\date{}

% ===========================================================================
\begin{document}
% ===========================================================================

\maketitle

% ---------------------------------------------------------------------------
\begin{abstract}
\placeholder{Write abstract after all experimental results are available.}
\end{abstract}

\tableofcontents
\newpage

% ---------------------------------------------------------------------------
% Notation table
% ---------------------------------------------------------------------------
\section*{Notation}
\label{sec:notation}
\addcontentsline{toc}{section}{Notation}

\begin{table}[H]
  \centering
  \small
  \begin{tabular}{lll}
    \toprule
    Symbol & Dimension & Meaning \\
    \midrule
    $\mathbf{s}_t$ & $\mathbb{R}^{124}$ & Rod state vector at time step $t$ \\
    $\mathbf{a}_t$ & $[-1,1]^5$ & Normalized CPG action vector \\
    $T$ & $\mathbb{R}^{124} \times \mathbb{R}^5 \to \mathbb{R}^{124}$ & Ground-truth transition operator (PyElastica) \\
    $f_\theta$ & $\mathbb{R}^{189} \to \mathbb{R}^{124}$ & Neural surrogate model with parameters $\theta$ \\
    $\mathbf{z}_t$ & $\mathbb{R}^{189}$ & Surrogate input (state $\|$ action $\|$ phase encoding) \\
    $\boldsymbol{\phi}_e$ & $\mathbb{R}^3$ & Per-element CPG phase triplet for element $e$ \\
    $\mathbf{x}(s,t)$ & $\mathbb{R}^3$ & Rod centerline position at arc length $s$ \\
    $\mathbf{d}(s,t)$ & $\mathbb{R}^3$ & Material director frame (orientation) \\
    $\mathbf{F}$ & $\mathbb{R}^3$ & Internal force (shear and tension) \\
    $\mathbf{M}$ & $\mathbb{R}^3$ & Internal moment (bending and torsion) \\
    $\kappa_e^{\text{target}}$ & $\mathbb{R}$ & Target curvature at element $e$ \\
    $\mu_\Delta, \sigma_\Delta$ & $\mathbb{R}^{124}$ & Training-set mean and std of state deltas \\
    $N$ & 21 & Number of discretization nodes \\
    $N_e$ & 20 & Number of discretization elements \\
    \bottomrule
  \end{tabular}
\end{table}

% ---------------------------------------------------------------------------
\section{Introduction}
\label{sec:intro}
% ---------------------------------------------------------------------------

Soft and continuum robots present a fundamental challenge for reinforcement learning: their dynamics are governed by distributed elasticity rather than rigid joints, and the resulting equations of motion are expensive to simulate \citep{Bing2019}.
A snake robot modeled as a Cosserat rod requires the resolution of elastic body dynamics, anisotropic ground friction, and internally generated muscle moments at every simulation substep.
PyElastica \citep{Naughton2021}, an open-source Cosserat rod simulator, provides this capability through a Position Verlet integrator with 50 internal substeps per control step.
The simulation is physically faithful, but computationally demanding.

On a 48-core server with 16 parallel workers, each environment step requires approximately 4.6\,ms, yielding a throughput of roughly 3{,}500 steps per second.
A proximal policy optimization (PPO) training run requiring $2 \times 10^6$ environment interactions therefore takes approximately 10 minutes of simulation wall-clock time, though the full training pipeline (gradient computation, logging, synchronization) extends this considerably.
Practical RL experimentation --- where multiple runs with different hyperparameters or random seeds are needed --- multiplies this cost proportionally.
This computational bottleneck is the central motivation for the present work.

The key observation underlying the surrogate approach is that the mapping from the current state and action to the next state --- the transition operator $T : (\mathbf{s}_t, \mathbf{a}_t) \mapsto \mathbf{s}_{t+1}$ --- is a deterministic function that can be approximated by a neural network.
If a learned surrogate $f_\theta \approx T$ can be evaluated in a single GPU forward pass rather than through 50 Position Verlet substeps, the per-step cost drops from ${\sim}4.6$\,ms to ${\sim}0.001$\,ms.
This represents a projected speedup of three to four orders of magnitude.
Such a speedup, if achieved at acceptable prediction accuracy, would make large-scale hyperparameter sweeps and multi-seed experiments practical.

A three-stage pipeline is presented in this report.
First, a parallel data collection system generates a large, diverse dataset of transition pairs $(\mathbf{s}_t, \mathbf{a}_t, \mathbf{s}_{t+1})$ from 16 independent PyElastica workers using Sobol quasi-random action sampling and state perturbation (\cref{sec:methods:datacollection}).
Second, a feedforward MLP surrogate is trained to predict the state delta $\mathbf{s}_{t+1} - \mathbf{s}_t$ from a 189-dimensional input comprising the 124-dimensional rod state, the 5-dimensional CPG action, and a 60-dimensional per-element phase encoding that provides the model with the exact phase of the serpenoid wave at each rod element (\cref{sec:methods:encoding,sec:methods:architecture}).
Third, the trained surrogate serves as a drop-in replacement for the PyElastica environment during PPO training, with a direct Elastica RL baseline providing the controlled comparison that isolates the policy gap attributable to surrogate modeling error (\cref{sec:experiments}).

The remainder of this report is organized as follows.
\Cref{sec:elastica} presents the Elastica backend: Cosserat rod dynamics, resistive force theory, CPG actuation, and the transition operator that the surrogate approximates.
\Cref{sec:dismech} describes the DisMech backend based on the Discrete Elastic Rods formulation.
\Cref{sec:related} reviews prior work on neural surrogates for soft robot dynamics and reinforcement learning with learned simulators.
\Cref{sec:methods} describes the data collection pipeline, state and action representations, per-element phase encoding, surrogate architecture, and the two-phase training procedure.
\Cref{sec:experiments} presents surrogate prediction accuracy, RL training performance, and the comparison with the direct Elastica baseline.
\Cref{sec:discussion} reflects on the physics calibration challenges, architecture experiments, and engineering lessons learned from the data collection pipeline.
\Cref{sec:conclusion} summarizes the findings and outlines directions for future work.

% ---------------------------------------------------------------------------
\section{Elastica Backend: Cosserat Rod Dynamics}
\label{sec:elastica}
% ---------------------------------------------------------------------------

% ---- System formulation: PDE → ODE → integrator ----

\subsection{System Formulation}
\label{sec:elastica:formulation}

The Elastica backend solves a well-posed initial--boundary value problem for the Cosserat rod \citep{Till2019,Naughton2021}.
The system is fully specified by the tuple $\mathcal{S} = (\mathcal{L},\; \Omega,\; \text{BC},\; \text{IC},\; \boldsymbol{\theta})$:

\begin{table}[H]
  \centering
  \small
  \renewcommand{\arraystretch}{1.3}
  \caption{Well-posedness tuple for the Elastica Cosserat rod system.}
  \label{tab:system-tuple}
  \begin{tabular}{clp{8.5cm}}
    \toprule
    Component & Name & Specification \\
    \midrule
    $\mathcal{L}$ & Governing equations &
      Cosserat rod PDEs: conservation of linear and angular momentum (\cref{eq:pde-linear,eq:pde-angular}) with linear elastic constitutive law (\cref{eq:constitutive}) \\
    $\Omega$ & Domain &
      $s \in [0, L]$, $L = 0.5$\,m (arc length) $\times$ $t \in [0, T]$ (time) \\
    BC & Boundary conditions &
      Free--free: $\mathbf{F} = \mathbf{M} = \mathbf{0}$ at $s = 0$ and $s = L$ \\
    IC & Initial conditions &
      Straight rod at rest: $\mathbf{x}(s,0) = \mathbf{x}_0 + s\hat{\mathbf{e}}_x$,\; $\mathbf{v} = \boldsymbol{\kappa} = \boldsymbol{\omega} = \mathbf{0}$ \\
    $\boldsymbol{\theta}$ & Parameters &
      $E{=}10^5$\,Pa, $\nu{=}0.5$, $\rho{=}1200$\,kg/m$^3$, $R{=}0.02$\,m, $c_t{=}0.01$, $c_n{=}0.05$, $\gamma{=}0.002$ \\
    Control & CPG forcing &
      $\mathbf{a} = (A, f, k, \phi_0, b) \in \mathbb{R}^5$ enters as time-varying rest curvature $\boldsymbol{\kappa}_0(s,t)$ \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{PDE (continuous).}

\begin{subequations}
\label{eq:cosserat-pdes}
\begin{alignat}{2}
  \rho A \,\frac{\partial \mathbf{v}}{\partial t}
    &= \frac{\partial \mathbf{F}}{\partial s} + \mathbf{f}_{\text{ext}}
    &\qquad& \text{(linear momentum)}
  \label{eq:pde-linear} \\[4pt]
  \rho \mathbf{I} \,\frac{\partial \boldsymbol{\omega}}{\partial t}
    &= \frac{\partial \mathbf{M}}{\partial s}
      + \mathbf{m}_{\text{ext}}
      + \frac{\partial \mathbf{x}}{\partial s} \times \mathbf{F}
    &\qquad& \text{(angular momentum)}
  \label{eq:pde-angular}
\end{alignat}
\end{subequations}

\noindent
Constitutive closure:

\begin{equation}
  \mathbf{F} = \mathbf{S}\,(\boldsymbol{\varepsilon} - \boldsymbol{\varepsilon}_0), \quad
  \mathbf{M} = \mathbf{B}\,(\boldsymbol{\kappa} - \boldsymbol{\kappa}_0), \quad
  \mathbf{S} = \operatorname{diag}(GA, GA, EA), \quad
  \mathbf{B} = \operatorname{diag}(EI, EI, GJ)
  \label{eq:constitutive}
\end{equation}

\paragraph{ODE (semi-discrete).}
Spatial discretization on a staggered grid ($N{=}21$ nodes, $N_e{=}20$ elements) replaces the PDE with 124 coupled first-order ODEs:

\begin{equation}
  \dot{\mathbf{s}} = \mathbf{g}(\mathbf{s},\, \mathbf{a},\, t), \qquad
  \mathbf{s} \in \mathbb{R}^{124},\;\; \mathbf{a} \in \mathbb{R}^{5}
  \label{eq:ode-system}
\end{equation}

\paragraph{Integrator (fully discrete).}
Position Verlet ($\Delta t = 0.001$\,s, $n_{\text{sub}} = 500$ substeps per RL step of $\Delta t_{\text{ctrl}} = 0.5$\,s) advances the ODE to produce the transition operator:

\begin{equation}
  \mathbf{s}_{t+1} = T(\mathbf{s}_t,\, \mathbf{a}_t)
    = \underbrace{V_{\Delta t}(\,\cdot\,;\, \boldsymbol{\kappa}^{\text{rest},49}) \circ \cdots \circ V_{\Delta t}(\,\cdot\,;\, \boldsymbol{\kappa}^{\text{rest},0})}_{50 \text{ substeps, each with distinct rest curvature}}(\mathbf{s}_t)
  \label{eq:transition-operator}
\end{equation}

\noindent
\Cref{alg:elastica-transition} expands this operator into its full computational form.
The 2D restriction ($z \equiv 0$) is exact for planar locomotion.

% ---- State vector ----

\begin{table}[H]
  \centering
  \small
  \caption{State vector $\mathbf{s}_t \in \mathbb{R}^{124}$ layout (Elastica).}
  \label{tab:state-vector}
  \begin{tabular}{llrl}
    \toprule
    Slice & Variable & Count & Meaning \\
    \midrule
    \texttt{[0:21]}    & $x_i$          & 21 & Node $x$-positions (m)                 \\
    \texttt{[21:42]}   & $y_i$          & 21 & Node $y$-positions (m)                 \\
    \texttt{[42:63]}   & $\dot{x}_i$    & 21 & Node $x$-velocities (m\,s$^{-1}$)     \\
    \texttt{[63:84]}   & $\dot{y}_i$    & 21 & Node $y$-velocities (m\,s$^{-1}$)     \\
    \texttt{[84:104]}  & $\psi_e$       & 20 & Element yaw angle (rad)                \\
    \texttt{[104:124]} & $\omega_{z,e}$ & 20 & Element angular velocity (rad\,s$^{-1}$) \\
    \bottomrule
  \end{tabular}
\end{table}

% ---- Staggered grid subsection ----

\subsection{Staggered Grid Discretization}
\label{sec:elastica:grid}

The continuous Cosserat rod is discretized on a \textbf{staggered grid} with three interleaved layers (\cref{fig:staggered-grid}).
This structure explains the decomposition of the 124-dimensional state vector in \cref{tab:state-vector}.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{../figures/staggered_grid.pdf}
  \caption{The three-layer staggered grid.
    Nodes (blue circles) sit at the rod vertices; elements (red squares) span between adjacent nodes; Voronoi nodes (green diamonds) sit between adjacent elements.
    Arrows show the sparse matrix operators that transfer information between layers.}
  \label{fig:staggered-grid}
\end{figure}

\begin{table}[H]
  \centering
  \small
  \renewcommand{\arraystretch}{1.3}
  \caption{The three staggered-grid layers and their stored degrees of freedom.}
  \label{tab:grid-layers}
  \begin{tabular}{lllrl}
    \toprule
    Layer & Index & DOF & Variables & Total \\
    \midrule
    Nodes    & $i = 1, \ldots, 21$ & Translational
      & $x_i, y_i, \dot{x}_i, \dot{y}_i$ & 84 \\
    Elements & $e = 1, \ldots, 20$ & Rotational
      & $\psi_e, \omega_{z,e}$ & 40 \\
    Voronoi  & $v = 1, \ldots, 19$ & Derived
      & $\kappa_v, \kappa_v^{\text{rest}}$ & 0 \\
    \midrule
    \multicolumn{4}{l}{\textbf{Total independent state variables}} & \textbf{124} \\
    \bottomrule
  \end{tabular}
\end{table}

\noindent
Nodes are the rod vertices; elements are the segments connecting adjacent nodes; Voronoi nodes sit between adjacent elements and are the natural location for computing curvature $\kappa_v$ --- the quantity that couples the CPG control input to the rod's elastic response.
Voronoi nodes carry no independent state; their quantities are derived from element values.

Four sparse matrix operators move information between layers:

\begin{table}[H]
  \centering
  \small
  \renewcommand{\arraystretch}{1.2}
  \caption{Sparse operators connecting the three staggered-grid layers.  Each has $O(N)$ nonzero entries.}
  \label{tab:grid-operators}
  \begin{tabular}{cllll}
    \toprule
    Operator & Direction & Size & Structure & Physical role \\
    \midrule
    $\mathbf{D}$ & Node $\to$ Element & $20 \times 21$ & Bidiagonal & Position differences $\to$ strain \\
    $\mathbf{D}^T$ & Element $\to$ Node & $21 \times 20$ & Bidiagonal & Internal stress $\to$ nodal forces \\
    $\mathbf{A}_v$ & Element $\to$ Voronoi & $19 \times 20$ & Tridiagonal & Element averages $\to$ curvature \\
    $\mathbf{D}_v$ & Voronoi $\to$ Element & $20 \times 19$ & Bidiagonal & Bending couple gradient $\to$ torques \\
    \bottomrule
  \end{tabular}
\end{table}

% ---- Action vector ----

\begin{table}[H]
  \centering
  \small
  \caption{Action vector $\mathbf{a}_t \in \mathbb{R}^{5}$: CPG wave parameters.}
  \label{tab:action-vector}
  \begin{tabular}{clrl}
    \toprule
    Symbol & Name & Range & Units \\
    \midrule
    $A$      & Amplitude    & $[0, 5]$      & rad\,m$^{-1}$ \\
    $f$      & Frequency    & $[0.5, 3.0]$  & Hz \\
    $k$      & Wave number  & $[0.5, 3.5]$  & wavelengths/body \\
    $\phi_0$ & Phase offset & $[0, 2\pi]$   & rad \\
    $b$      & Turn bias    & $[-2, 2]$     & rad\,m$^{-1}$ \\
    \bottomrule
  \end{tabular}
\end{table}

% ---- Single unified algorithm ----

\begin{algorithm}[H]\small
\caption{Elastica Transition Operator $T(\mathbf{s}_t,\, \mathbf{a}_t,\, t_{\text{step}}) \to \mathbf{s}_{t+1}$}\label{alg:elastica-transition}
\vspace{2pt}
\noindent
\begin{minipage}[t]{0.28\linewidth}\scriptsize
\renewcommand{\arraystretch}{1.08}
\textbf{Ensure} \\[1pt]
Next state $\mathbf{s}_{t+1} \in \mathbb{R}^{124}$

\vspace{4pt}
\textbf{Parameters} \\[1pt]
$\Delta t {=} 0.001$\,s -- substep \\
$n_{\text{sub}} {=} 500$ -- substeps/step \\
$\Delta t_{\text{ctrl}} {=} 0.5$\,s -- RL step \\
$\mathbf{S} \in \mathbb{R}^{3 \times 3}$ -- shear--stretch \\
$\mathbf{B} \in \mathbb{R}^{3 \times 3}$ -- bend--twist \\
$c_t, c_n \in \mathbb{R}$ -- RFT friction \\
$\gamma \in \mathbb{R}$ -- damping \\
$m_i \in \mathbb{R}$ -- mass at node $i$ \\
$J_e \in \mathbb{R}$ -- inertia of elem $e$ \\
$\mathbf{g} \in \mathbb{R}^{2}$ -- gravity

\vspace{4pt}
\textbf{State} \\[1pt]
$\mathbf{q} \in \mathbb{R}^{62}$ -- $(x_i, y_i, \psi_e)$ \\
$\dot{\mathbf{q}} \in \mathbb{R}^{62}$ -- $(\dot{x}_i, \dot{y}_i, \omega_{z,e})$ \\
$\mathbf{s} {=} (\mathbf{q}, \dot{\mathbf{q}}) \in \mathbb{R}^{124}$

\vspace{4pt}
\textbf{CPG} (\cref{tab:action-vector}) \\[1pt]
$A, f, k, \phi_0, b \in \mathbb{R}$ -- from $\mathbf{a}_t$ \\
$s_v \in [0,1]$ -- arc-length \\
$t^n \in \mathbb{R}$ -- substep time

\vspace{4pt}
\textbf{Grid ops} (\cref{tab:grid-operators}) \\[1pt]
$\mathbf{D} \in \mathbb{R}^{20 \times 21}$ -- node $\to$ elem \\
$\mathbf{D}^T \in \mathbb{R}^{21 \times 20}$ -- elem $\to$ node \\
$\mathbf{A}_v \in \mathbb{R}^{19 \times 20}$ -- elem $\to$ Vor. \\
$\mathbf{D}_v \in \mathbb{R}^{20 \times 19}$ -- Vor.\ $\to$ elem

\vspace{4pt}
\textbf{Intermediates} \\[1pt]
$\boldsymbol{\kappa}^{\text{rest}} \in \mathbb{R}^{19}$ -- target curv. \\
$\mathbf{q}^{1/2} \in \mathbb{R}^{62}$ -- half-step pos. \\
$\mathbf{t}_e \in \mathbb{R}^{20 \times 2}$ -- tangents \\
$\ell_e \in \mathbb{R}^{20}$ -- lengths \\
$\varepsilon_e \in \mathbb{R}^{20}$ -- stretch \\
$\hat{\varepsilon}_v \in \mathbb{R}^{19}$ -- Vor.\ dilatation \\
$\mathbf{Q}_e \in \mathbb{R}^{2 \times 2}$ -- rotation \\
$\boldsymbol{\sigma}_e \in \mathbb{R}^{20 \times 2}$ -- strain \\
$\mathbf{n}_e^L \in \mathbb{R}^{20 \times 2}$ -- stress \\
$\boldsymbol{\kappa}_v \in \mathbb{R}^{19}$ -- curvature \\
$\boldsymbol{\tau}_v^L \in \mathbb{R}^{19}$ -- bend couple \\
$\mathbf{F}^{\text{int}} \in \mathbb{R}^{21 \times 2}$ -- int.\ forces \\
$\mathbf{F}^{\text{ext}} \in \mathbb{R}^{21 \times 2}$ -- ext.\ forces \\
$\mathbf{m}^{\text{int}} \in \mathbb{R}^{20}$ -- int.\ torques \\
$\mathbf{m}^{\text{ext}} \in \mathbb{R}^{20}$ -- ext.\ torques \\
$\mathbf{v}_t, \mathbf{v}_n \in \mathbb{R}^{21 \times 2}$ -- vel. \\
$\ddot{\mathbf{q}} \in \mathbb{R}^{62}$ -- accelerations
\end{minipage}%
\hfill
\vrule
\hfill
\begin{minipage}[t]{0.68\linewidth}
\begin{algorithmic}[1]
\State $\mathbf{s} \gets \mathbf{s}_t$
\For{$n = 0, 1, \ldots, n_{\text{sub}}{-}1$}
  \Statex \hspace{\algorithmicindent}\textit{// Control input: PDE forcing term $\boldsymbol{\kappa}_0(s,t)$}
  \State $t^n \gets t_{\text{step}} + n \cdot \Delta t$
  \For{$v = 1, \ldots, 19$}
    \State $\kappa_v^{\text{rest}} \gets A \sin(2\pi k \cdot s_v + 2\pi f \cdot t^n + \phi_0) + b$
  \EndFor
  \Statex \hspace{\algorithmicindent}\textit{// Integrator: Verlet half-step}
  \State $\mathbf{q}^{1/2} \gets \mathbf{q} + \tfrac{1}{2}\Delta t\;\dot{\mathbf{q}}$
  \Statex \hspace{\algorithmicindent}\textit{// ODE right-hand side: discretized PDE}
  \Statex \hspace{\algorithmicindent}\textit{// --- geometry}
  \State $\Delta\mathbf{x}_e \gets \mathbf{D}\,\mathbf{x}^{1/2}$
  \State $\ell_e \gets \|\Delta\mathbf{x}_e\|$
  \State $\mathbf{t}_e \gets \Delta\mathbf{x}_e / \ell_e$
  \State $\varepsilon_e \gets \ell_e / \ell_e^{\text{rest}}$
  \Statex \hspace{\algorithmicindent}\textit{// --- shear--stretch $\to$ internal nodal forces}
  \State $\boldsymbol{\sigma}_e \gets \mathbf{Q}_e^T(\Delta\mathbf{x}_e / \ell_e^{\text{rest}})$
  \State $\mathbf{n}_e^L \gets \mathbf{S}\,(\boldsymbol{\sigma}_e - \boldsymbol{\sigma}_e^{\text{rest}})$
  \State $\mathbf{F}^{\text{int}} \gets \mathbf{D}^T[\mathbf{Q}_e\,\mathbf{n}_e^L / \varepsilon_e]$
  \Statex \hspace{\algorithmicindent}\textit{// --- bend--twist $\to$ internal element torques}
  \State $\boldsymbol{\kappa}_v \gets \text{curvature from directors at Voronoi}$
  \State $\boldsymbol{\tau}_v^L \gets \mathbf{B}\,(\boldsymbol{\kappa}_v - \boldsymbol{\kappa}^{\text{rest}})$
  \State $\mathbf{m}^{\text{int}} \gets \mathbf{D}_v[\boldsymbol{\tau}_v^L / \hat{\varepsilon}_v^3]
    + \mathbf{A}_v[(\boldsymbol{\kappa}_v {\times} \boldsymbol{\tau}_v^L) \hat{\ell}_v / \hat{\varepsilon}_v^3]$
  \Statex \hspace{\algorithmicindent}\hspace{\algorithmicindent}$\quad + (\mathbf{Q}_e\mathbf{t}_e) {\times} \mathbf{n}_e^L \ell_e^{\text{rest}}
    + \ldots$
  \Statex \hspace{\algorithmicindent}\textit{// --- external forces}
  \State $\mathbf{F}^{\text{ext}} \gets -c_t \mathbf{v}_t - c_n \mathbf{v}_n -\gamma\dot{\mathbf{q}} + m_i\mathbf{g}$
  \Statex \hspace{\algorithmicindent}\textit{// --- accelerations}
  \State $(\ddot{x}_i, \ddot{y}_i) \gets (\mathbf{F}^{\text{int}}_i + \mathbf{F}^{\text{ext}}_i) / m_i$
  \State $\dot{\omega}_{z,e} \gets (\mathbf{m}^{\text{int}}_e + \mathbf{m}^{\text{ext}}_e) / (\varepsilon_e J_e)$
  \Statex \hspace{\algorithmicindent}\textit{// Integrator: Verlet velocity update $+$ full-step}
  \State $\dot{\mathbf{q}} \gets \dot{\mathbf{q}} + \Delta t\;\ddot{\mathbf{q}}$
  \State $\mathbf{q} \gets \mathbf{q}^{1/2} + \tfrac{1}{2}\Delta t\;\dot{\mathbf{q}}$
\EndFor
\State \textbf{return} $\mathbf{s} = (\mathbf{q},\, \dot{\mathbf{q}})$
\end{algorithmic}
\vspace{4pt}
\noindent\rule{\linewidth}{0.4pt}
\vspace{2pt}
\textit{Cost:} ${\sim}754$ scalar ops/substep $\times$ 50 $= {\sim}37{,}700$ ops/step (${\sim}4.6$\,ms on CPU). \\
\textit{Key:} $\boldsymbol{\kappa}^{\text{rest}}$ changes every substep --- the CPG phase advances by $\Delta\phi = 2\pi f \Delta t$ per substep, accumulating $2\pi f \cdot \Delta t_{\text{ctrl}}$ radians over one RL step. The surrogate must learn the \textit{integrated effect} of this traveling wave, not a single static curvature.
\end{minipage}
\end{algorithm}

% ---- Surrogate approximation target ----

The surrogate replaces the entire operator $T$ with a single forward pass:

\begin{equation}
  \underbrace{\text{Cosserat PDE}}_{\text{continuous}}
    \;\xrightarrow{\;\epsilon_{\text{space}}\;}
  \underbrace{\dot{\mathbf{s}} = \mathbf{g}(\mathbf{s}, \mathbf{a}, t)}_{\text{124-dim ODE}}
    \;\xrightarrow{\;\epsilon_{\text{time}}\;}
  \underbrace{T = V_{\Delta t}^{50}}_{\text{Alg.~\ref{alg:elastica-transition}}}
    \;\xrightarrow{\;\epsilon_{\text{nn}}\;}
  \underbrace{f_\theta}_{\text{surrogate}}
  \label{eq:approximation-chain}
\end{equation}

\begin{equation}
  f_\theta(\mathbf{z}_t) \;\approx\; T(\mathbf{s}_t,\, \mathbf{a}_t) - \mathbf{s}_t, \qquad
  \mathbf{z}_t \in \mathbb{R}^{189},\;\;
  f_\theta \colon \mathbb{R}^{189} \to \mathbb{R}^{124}
  \label{eq:nn-approximation}
\end{equation}

\begin{table}[H]
  \centering
  \small
  \renewcommand{\arraystretch}{1.3}
  \caption{Simulator vs.\ surrogate: input/output comparison.}
  \label{tab:surrogate-io}
  \begin{tabular}{llc}
    \toprule
    & \textbf{Quantity} & \textbf{Shape} \\
    \midrule
    \multirow{3}{*}{\textbf{Simulator ($T$)}}
      & Input: $\mathbf{s}_t$ (state) $+$ $\mathbf{a}_t$ (action) $+$ $t_{\text{step}}$ (time) & $\mathbb{R}^{124} + \mathbb{R}^{5} + \mathbb{R}$ \\
      & Output: $\mathbf{s}_{t+1}$ & $\mathbb{R}^{124}$ \\
      & Cost: ${\sim}37{,}700$ ops (${\sim}4.6$\,ms CPU) & \\
    \midrule
    \multirow{3}{*}{\textbf{Surrogate ($f_\theta$)}}
      & Input: $\mathbf{z}_t = [\bar{\mathbf{s}}_t \;\|\; \mathbf{a}_t \;\|\; \boldsymbol{\phi}_1, \ldots, \boldsymbol{\phi}_{20}]$ & $\mathbb{R}^{189}$ \\
      & Output: $\hat{\mathbf{s}}_{t+1} = \mathbf{s}_t + \boldsymbol{\sigma}_\Delta \odot f_\theta(\mathbf{z}_t) + \boldsymbol{\mu}_\Delta$ & $\mathbb{R}^{124}$ \\
      & Cost: single forward pass (${\sim}0.001$\,ms GPU) & \\
    \bottomrule
  \end{tabular}
\end{table}

% ---------------------------------------------------------------------------
\section{DisMech Backend: Discrete Elastic Rods}
\label{sec:dismech}
% ---------------------------------------------------------------------------

The second physics backend replaces PyElastica's explicit symplectic integration with DisMech, a Discrete Elastic Rod (DER) simulator based on the formulation of \citet{Bergou2008,Bergou2010}.
The key difference is the time integration scheme: DisMech uses implicit backward Euler, which is unconditionally stable and permits a single integration step of $\Delta t = 0.05$\,s per RL action --- eliminating the 50-substep inner loop required by Elastica's explicit integrator.
This section presents the DER discretization, the implicit time integration, and the resulting transition operator that the surrogate approximates.

% ---- System formulation ----

\subsection{System Formulation}
\label{sec:dismech:formulation}

The continuous Cosserat rod PDEs (\cref{eq:pde-linear,eq:pde-angular}) apply identically to the DisMech backend; both simulators solve the same governing equations with the same constitutive law (\cref{eq:constitutive}).
The system tuple $\mathcal{S} = (\mathcal{L}, \Omega, \text{BC}, \text{IC}, \boldsymbol{\theta})$ matches \cref{tab:system-tuple} in all entries except the solver parameters $\boldsymbol{\theta}$: DisMech replaces the explicit substep size $\Delta t = 0.001$\,s and damping $\gamma = 0.002$ with Newton solver tolerances $\epsilon_f = 10^{-4}$, $\epsilon_d = 10^{-2}$, and a maximum of 25 iterations per timestep.

\textbf{Primary unknowns.}
DER uses vertex positions and a material frame (Bishop frame) as the primary unknowns, rather than the directors used in PyElastica.
At each internal vertex $i$ the discrete curvature binormal is $\boldsymbol{\kappa}_i = 2 \mathbf{e}_{i-1} \times \mathbf{e}_i / (|\mathbf{e}_{i-1}||\mathbf{e}_i| + \mathbf{e}_{i-1} \cdot \mathbf{e}_i)$, where $\mathbf{e}_i = \mathbf{x}_{i+1} - \mathbf{x}_i$ is the edge vector.
The discrete bending energy at vertex $i$ is:
\begin{equation}
  E_{\text{bend},i}
    = \frac{1}{2\bar{\ell}_i}
      (\boldsymbol{\kappa}_i - \boldsymbol{\kappa}_i^{\text{rest}})^\top
      \mathbf{B}\,
      (\boldsymbol{\kappa}_i - \boldsymbol{\kappa}_i^{\text{rest}}),
  \label{eq:dismech-bend-energy}
\end{equation}
where $\bar{\ell}_i$ is the Voronoi length (mean of adjacent edge lengths) and $\boldsymbol{\kappa}_i^{\text{rest}}$ is the rest curvature that serves as the control input.

\textbf{State vector.}
The state vector uses the identical 124-dimensional layout shown in \cref{tab:state-vector}.
Positions are extracted from the 3D state vector $\mathbf{q} \in \mathbb{R}^{63}$ by taking only the $(x, y)$ components (the 2D constraint sets $z \equiv 0$).
Yaw angles $\psi_e$ are computed from segment tangent vectors $\psi_e = \text{atan2}(y_{i+1} - y_i,\, x_{i+1} - x_i)$ rather than being stored directly, and angular velocities $\omega_{z,e}$ are derived from the cross product of tangent and velocity difference vectors: $\omega_{z,e} = (\Delta x_e \Delta\dot{y}_e - \Delta y_e \Delta\dot{x}_e) / |\Delta\mathbf{x}_e|^2$.

% ---- DER discretization ----

\subsection{Discrete Elastic Rod Discretization}
\label{sec:dismech:der}

The DER discretization follows a vertex--edge--hinge structure analogous to the staggered grid in \cref{sec:elastica:grid}.
Vertices store positions $\mathbf{x}_i \in \mathbb{R}^3$ (analogous to nodes in the Elastica grid).
Edges connect adjacent vertices and carry the tangent direction and stretch strain.
Hinges (analogous to Voronoi nodes) sit between adjacent edges and are where discrete curvature is computed.

The three-layer staggered grid (\cref{fig:staggered-grid}) applies to both backends; DisMech terms the middle layer \textit{edges} and the curvature layer \textit{hinges}.
The same sparse operators (\cref{tab:grid-operators}) transfer information between layers, with DisMech implementing them via direct array indexing rather than sparse matrix--vector products.

\textbf{Action vector.}
The CPG action parameters are identical to the Elastica backend (\cref{tab:action-vector}): five parameters $(A, f, k, \phi_0, b)$ that define the serpenoid curvature wave.

\textbf{Control mechanism.}
DisMech applies curvature control through the natural strain of discrete bend springs.
Setting $\texttt{bend\_springs.nat\_strain}[i, 0] = \kappa_i^{\text{rest}}$ modifies the rest curvature that the elastic energy (\cref{eq:dismech-bend-energy}) drives the rod toward --- the same physical mechanism as PyElastica's $\texttt{rest\_kappa}$ array, but accessed through a different API.

% ---- Implicit time integration ----

\subsection{Implicit Time Integration}
\label{sec:dismech:implicit}

\textbf{Backward Euler formulation.}
DisMech discretizes the equations of motion using backward Euler.
Given positions $\mathbf{q}_n$ and velocities $\mathbf{v}_n$ at time $t_n$, the update rule is:
\begin{align}
  \mathbf{v}_{n+1} &= \mathbf{v}_n + \Delta t\,\mathbf{M}^{-1}\mathbf{F}(\mathbf{q}_{n+1},\, \mathbf{v}_{n+1}), \label{eq:dismech-be-vel} \\
  \mathbf{q}_{n+1} &= \mathbf{q}_n + \Delta t\,\mathbf{v}_{n+1}, \label{eq:dismech-be-pos}
\end{align}
where $\mathbf{M}$ is the mass matrix and $\mathbf{F}$ includes all internal elastic forces and external forces (gravity, friction).
Because $\mathbf{F}$ is evaluated at the unknown future state $(\mathbf{q}_{n+1}, \mathbf{v}_{n+1})$, the system is implicit.

\textbf{Nonlinear residual.}
Substituting \cref{eq:dismech-be-pos} into \cref{eq:dismech-be-vel} and rearranging yields:
\begin{equation}
  \mathbf{g}(\mathbf{q}_{n+1})
    \coloneqq \mathbf{M}\bigl(\mathbf{q}_{n+1} - 2\mathbf{q}_n + \mathbf{q}_{n-1}\bigr)
    - \Delta t^2\,\mathbf{F}(\mathbf{q}_{n+1})
    = \mathbf{0}.
  \label{eq:dismech-residual}
\end{equation}

\textbf{Newton solver.}
Each timestep solves \cref{eq:dismech-residual} via Newton iterations:
\begin{equation}
  \mathbf{q}^{(k+1)} = \mathbf{q}^{(k)} - \mathbf{J}^{-1}(\mathbf{q}^{(k)})\,\mathbf{g}(\mathbf{q}^{(k)}),
  \label{eq:dismech-newton}
\end{equation}
where $\mathbf{J} = \partial\mathbf{g}/\partial\mathbf{q}$ is the sparse Jacobian (tangent stiffness matrix plus mass contributions).
The linear system is solved via a direct sparse factorization.
Convergence is declared when $\|\mathbf{g}\| < \epsilon_f = 10^{-4}$ (force tolerance) or $\|\delta\mathbf{q}\| < \epsilon_d = 10^{-2}$ (displacement tolerance), with a maximum of 25 iterations.

\textbf{Comparison with Elastica.}
\Cref{tab:dismech-comparison} summarizes the key differences between the two integration approaches.

\begin{table}[H]
  \centering
  \small
  \renewcommand{\arraystretch}{1.3}
  \caption{Comparison of time integration approaches: PyElastica (explicit) vs.\ DisMech (implicit).}
  \label{tab:dismech-comparison}
  \begin{tabular}{@{}lll@{}}
    \toprule
    \textbf{Property} & \textbf{PyElastica (Explicit)} & \textbf{DisMech (Implicit)} \\
    \midrule
    Integration scheme
      & Symplectic (Verlet / PEFRL)
      & Backward Euler (Newton) \\
    Timestep per call
      & $\Delta t = 0.001$\,s
      & $\Delta t = 0.05$\,s \\
    Steps per second
      & 1000
      & 20 \\
    Stability
      & Conditional ($\Delta t < \Delta t^{\max}$)
      & Unconditional \\
    Substep loop
      & 50 substeps per RL step
      & 1 step per RL step \\
    Cost per step
      & ${\sim}4.6$\,ms CPU (low per-step, many steps)
      & ${\sim}12$\,ms CPU (single Newton solve) \\
    Energy behavior
      & Bounded drift (symplectic)
      & Numerical dissipation (implicit) \\
    Curvature control
      & \texttt{rest\_kappa} array
      & Natural strain of bend springs \\
    Solver parameters
      & $\Delta t_{\text{sub}}$, $\gamma$ (damping)
      & $\epsilon_f$, $\epsilon_d$, max\_iter \\
    \bottomrule
  \end{tabular}
\end{table}

% ---- Algorithm ----

\begin{algorithm}[H]\small
\caption{DisMech Transition Operator $T_{\text{DER}}(\mathbf{s}_t,\, \mathbf{a}_t,\, t_{\text{step}}) \to \mathbf{s}_{t+1}$}\label{alg:dismech-transition}
\vspace{2pt}
\noindent
\begin{minipage}[t]{0.28\linewidth}\scriptsize
\renewcommand{\arraystretch}{1.08}
\textbf{Ensure} \\[1pt]
Next state $\mathbf{s}_{t+1} \in \mathbb{R}^{124}$

\vspace{4pt}
\textbf{Parameters} \\[1pt]
$\Delta t {=} 0.05$\,s -- timestep \\
$\epsilon_f {=} 10^{-4}$ -- force tol. \\
$\epsilon_d {=} 10^{-2}$ -- disp. tol. \\
max\_iter $= 25$ \\
$\mathbf{B} \in \mathbb{R}^{2 \times 2}$ -- bending stiffness \\
$\mu$ -- friction coeff.

\vspace{4pt}
\textbf{State} \\[1pt]
$\mathbf{q} \in \mathbb{R}^{63}$ -- positions \\
\quad(21 nodes $\times$ 3D, $z{=}0$) \\
$\mathbf{v} \in \mathbb{R}^{63}$ -- velocities

\vspace{4pt}
\textbf{CPG} (\cref{tab:action-vector}) \\[1pt]
$A, f, k, \phi_0, b \in \mathbb{R}$ \\
$s_j \in [0,1]$ -- arc-length \\
$t_{\text{step}} \in \mathbb{R}$ -- RL time
\end{minipage}%
\hfill
\vrule
\hfill
\begin{minipage}[t]{0.68\linewidth}
\begin{algorithmic}[1]
\Statex \textit{// Step 1: Set curvature (once per RL step)}
\State $\omega \gets 2\pi f$
\For{$j = 1, \ldots, 19$}
  \State $\kappa_j^{\text{rest}} \gets A \sin(2\pi k \cdot s_j + \omega \cdot t_{\text{step}} + \phi_0) + b$
  \State $\texttt{bend\_springs.nat\_strain}[j, 0] \gets \kappa_j^{\text{rest}}$
\EndFor
\Statex
\Statex \textit{// Step 2: Initial guess (explicit Euler predictor)}
\State $\mathbf{q}^{(0)} \gets \mathbf{q} + \Delta t\,\mathbf{v}$
\Statex
\Statex \textit{// Step 3: Newton iterations}
\For{$k = 0, 1, \ldots, \text{max\_iter} - 1$}
  \State Assemble residual $\mathbf{g}(\mathbf{q}^{(k)}) \gets \mathbf{M}(\mathbf{q}^{(k)} - 2\mathbf{q} + \mathbf{q}_{\text{prev}}) - \Delta t^2\,\mathbf{F}(\mathbf{q}^{(k)})$
  \State Assemble Jacobian $\mathbf{J} \gets \partial\mathbf{g}/\partial\mathbf{q}\big|_{\mathbf{q}^{(k)}}$
  \State Solve $\delta\mathbf{q} \gets \mathbf{J}^{-1}\mathbf{g}$ \Comment{Sparse direct solve}
  \State $\mathbf{q}^{(k+1)} \gets \mathbf{q}^{(k)} - \delta\mathbf{q}$
  \If{$\|\mathbf{g}\| < \epsilon_f$ \textbf{or} $\|\delta\mathbf{q}\| < \epsilon_d$}
    \State \textbf{break} \Comment{Converged}
  \EndIf
\EndFor
\Statex
\Statex \textit{// Step 4: Update velocity}
\State $\mathbf{v}' \gets (\mathbf{q}^{(K)} - \mathbf{q}) / \Delta t$
\Statex
\Statex \textit{// Step 5: Pack 2D state}
\State Extract $(x_i, y_i, \dot{x}_i, \dot{y}_i)$ from 3D $(\mathbf{q}', \mathbf{v}')$
\State Compute $\psi_e = \text{atan2}(\Delta y_e, \Delta x_e)$ and $\omega_{z,e}$
\State \textbf{return} $\mathbf{s}_{t+1} = (x, y, \dot{x}, \dot{y}, \psi, \omega_z) \in \mathbb{R}^{124}$
\end{algorithmic}
\vspace{4pt}
\noindent\rule{\linewidth}{0.4pt}
\vspace{2pt}
\textit{Cost:} 1 Newton solve with ${\sim}3$--$5$ iterations, ${\sim}12$\,ms on CPU (sparse direct factorization). No substep loop.
\end{minipage}
\end{algorithm}

% ---- Surrogate approximation target ----

The surrogate approximation chain for DisMech mirrors the Elastica version with a different integration step:

\begin{equation}
  \underbrace{\text{Cosserat PDE}}_{\text{continuous}}
    \;\xrightarrow{\;\epsilon_{\text{space}}\;}
  \underbrace{\dot{\mathbf{s}} = \mathbf{g}_{\text{DER}}(\mathbf{s}, \mathbf{a}, t)}_{\text{124-dim ODE}}
    \;\xrightarrow{\;\epsilon_{\text{time}}\;}
  \underbrace{T_{\text{DER}} = \text{Newton}(\Delta t)}_{\text{Alg.~\ref{alg:dismech-transition}}}
    \;\xrightarrow{\;\epsilon_{\text{nn}}\;}
  \underbrace{f_\theta}_{\text{surrogate}}
  \label{eq:dismech-approximation-chain}
\end{equation}

The surrogate input/output structure is identical to \cref{tab:surrogate-io}; only the simulator cost changes from ${\sim}4.6$\,ms (Elastica, 50 substeps) to ${\sim}12$\,ms (DisMech, single Newton solve).
The neural network architecture, dataset format, and training procedure require no modification.

\textbf{Backend-agnostic surrogate.}
The same MLP that learns Elastica dynamics can be retrained on DisMech data without any change to the network, dataset format, or training procedure.
The only adaptation is in the state extraction layer that packs DisMech's 3D vertex state into the shared 124-dimensional representation.
This backend-agnostic design enables direct comparison of surrogate fidelity across physics engines: differences in prediction accuracy reflect differences in the underlying dynamics (explicit vs.\ implicit integration, symplectic vs.\ dissipative energy behavior) rather than architectural artifacts.

% ---------------------------------------------------------------------------
\section{Related Work}
\label{sec:related}
% ---------------------------------------------------------------------------

\subsection{Neural Surrogates for Soft Robot Dynamics}
\label{sec:related:surrogates}

The use of neural networks to approximate the dynamics of elastic and soft robots has been investigated as a means of bypassing the computational cost of first-principles simulation.
Several results are directly relevant to the Cosserat rod setting considered here.

\citet{Stolzle2025} propose a domain-decoupled physics-informed neural network (DD-PINN) for Cosserat rod dynamics.
By decoupling the time domain from the feedforward network, gradients are computed in closed form rather than through automatic differentiation over the ODE integrator.
The resulting model achieves a $44{,}000\times$ speedup over numerical Cosserat rod integration, with end-effector position errors below 3\,mm (2.3\% of actuator length) across the full 72-dimensional rod state.
This performance enables nonlinear model-predictive control at 70\,Hz on a GPU --- a capability that is infeasible with direct numerical integration.

A hybrid approach is taken in KNODE-Cosserat \citep{Hsieh2024}, where a simplified first-principles Cosserat rod model serves as the backbone and a neural ODE residual term corrects the systematic discrepancy between the simplified physics and the true behavior.
Training data for this model is collected from PyElastica simulations --- a pipeline analogous to the one used here.
The hybrid model achieves a 58.7\% accuracy improvement over the physics-only model on long-horizon trajectories, demonstrating that even a coarse physics prior reduces the learning burden on the neural correction term.

The most closely related prior work is SoRoLEX \citep{SoRoLEX2024}, which trains an LSTM forward dynamics model on soft robot simulator data and deploys it as a surrogate environment for PPO training with JAX-based GPU parallelization.
The pipeline --- collect trajectories from a high-fidelity simulator, train a neural dynamics model, run large-scale RL in the learned environment --- is identical in structure to the approach presented here.
The differences lie in the architecture (MLP rather than LSTM), the prediction target (the full 124-dimensional Cosserat rod state rather than a task-space observation), and the explicit use of a multi-step rollout loss to limit autoregressive error accumulation.

Physics-informed approaches have also been applied to articulated soft robots.
\citet{PINNSoftRobot2025} demonstrate a $467\times$ speedup over first-principles models with MPC at 47\,Hz, requiring as little as a single training dataset.
This illustrates the data efficiency advantages that follow from incorporating physical structure into the surrogate architecture.

These results collectively establish that neural surrogates for Cosserat rod dynamics are feasible and capable of sustaining the multi-step rollout accuracy required for closed-loop control.
The present work follows the data-driven approach of SoRoLEX, extended with per-element CPG phase encoding and a two-phase training schedule that combines single-step MSE with multi-step rollout loss.

% ---------------------------------------------------------------------------
\section{Methods}
\label{sec:methods}
% ---------------------------------------------------------------------------

\subsection{Data Collection Pipeline}
\label{sec:methods:datacollection}

Training data for the surrogate model is generated by running 16 independent worker processes, each maintaining its own PyElastica Cosserat rod simulation instance.
Actions are sampled from the 5-dimensional CPG action space using a Sobol quasi-random sequence, which distributes points more uniformly across all dimensions than pseudo-random sampling.
To populate diverse regions of the state space, 30\% of episodes begin with a state perturbation: random offsets are applied to node positions, and angular velocity noise is injected into the rod elements with $\sigma_\omega = 1.5$\,rad/s (see \cref{sec:discussion:pipeline} for the rationale behind this value).
Within each episode, 50\% of actions are drawn uniformly at random and the remaining 50\% follow the Sobol sequence.

\begin{algorithm}[H]
\caption{Parallel data collection for surrogate training}
\label{alg:data-collection}
\begin{algorithmic}[1]
  \Require Number of workers $W = 16$, Sobol sequence $\mathcal{S}$, perturbation fraction $p = 0.3$
  \Ensure Dataset $\mathcal{D} = \{(\mathbf{s}_t, \mathbf{a}_t, \mathbf{s}_{t+1})\}$
  \For{each worker $w = 1, \ldots, W$ in parallel}
    \While{not done}
      \State Initialize rod; with probability $p$, perturb state
      \State Sample action $\mathbf{a}$ from $\mathcal{S}$ (50\%) or uniform (50\%)
      \State Execute one RL step: $\mathbf{s}_{t+1} \gets T(\mathbf{s}_t, \mathbf{a}_t)$
      \State Store $(\mathbf{s}_t, \mathbf{a}_t, \mathbf{s}_{t+1}, t_{\text{start}})$ to batch file
    \EndWhile
  \EndFor
\end{algorithmic}
\end{algorithm}

The collection format stores one transition per row: the current state $\mathbf{s}_t$, the action $\mathbf{a}_t$, the next state $\mathbf{s}_{t+1}$, the episode start time $t_{\text{start}}$, and optionally the internal and external forces and torques acting on the rod.
At approximately 2055 bytes per transition including forces, the target dataset size of 10\,GB yields roughly 5 million transition pairs.
A health monitor tracks the output rate of each worker and respawns any worker that stalls after initialization, ensuring sustained throughput across multi-hour collection runs.

\subsection{State and Action Representation}
\label{sec:methods:representation}

The full 124-dimensional rod state vector $\mathbf{s}_t \in \mathbb{R}^{124}$, defined in \cref{tab:state-vector}, serves as the Markov state for both surrogate training and RL.
The 2D projection from the 3D Cosserat state is exact --- $z$-components are identically zero --- so no physical content is lost.

The action vector $\mathbf{a}_t \in [-1,1]^5$ is linearly mapped to the physical CPG parameter ranges defined in \cref{tab:action-vector}:

\begin{equation}
  \mathbf{a}_{\text{phys}} = \frac{1}{2}\bigl[(\mathbf{a}_t + 1) \odot (\mathbf{a}_{\max} - \mathbf{a}_{\min}) + 2\,\mathbf{a}_{\min}\bigr]
  \label{eq:action-mapping}
\end{equation}

\begin{itemize}
  \item $\mathbf{a}_t \in [-1,1]^5$ --- normalized action from the RL policy
  \item $\mathbf{a}_{\max}$, $\mathbf{a}_{\min}$ --- upper and lower bounds of each CPG parameter
  \item $\odot$ --- element-wise multiplication
\end{itemize}
Denormalization to physical units occurs only inside the PyElastica environment; the surrogate receives $\mathbf{a}_t$ directly.

\subsection{Per-Element CPG Phase Encoding}
\label{sec:methods:encoding}

The target curvature at element $e$ depends on both the global oscillation phase $2\pi f t$ and the element's arc-length position $s_e$ through the serpenoid wave (\cref{alg:elastica-transition}, line~5).
Elements at opposite ends of the rod can be at opposite phases of the curvature wave at the same instant.
An early version of the surrogate (V1) used a global phase encoding $[\sin(2\pi f t), \cos(2\pi f t)] \in \mathbb{R}^2$ appended once to the input, requiring the model to reconstruct per-element phase offsets implicitly through shared MLP weights.
This imposed a significant learning burden and contributed to poor angular velocity predictions (R$^2 = 0.23$).

The V2 encoding provides the per-element phase explicitly.
For element $e \in \{1, \ldots, N_e\}$, an effective time is computed as:

\begin{equation}
  t_e = \frac{k \cdot s_e}{f}
  \label{eq:effective-time}
\end{equation}

From this, a phase triplet is constructed:

\begin{equation}
  \boldsymbol{\phi}_e
    = \bigl(\sin(2\pi f \cdot t_e),\;
            \cos(2\pi f \cdot t_e),\;
            \kappa_e^{\text{actual}}\bigr)
  \label{eq:per-element-phase}
\end{equation}

The effective time $t_e = k \cdot s_e / f$ incorporates the spatial phase offset at element $e$, where $k$ is the wave number and $s_e$ is the normalized arc-length position.
The sine and cosine terms give the model the exact phase of the serpenoid wave at each element's location, and $\kappa_e^{\text{actual}}$ is the instantaneous curvature providing the control error that drives the elastic restoring force.

Stacking $\boldsymbol{\phi}_e$ for all $N_e = 20$ elements yields a 60-dimensional phase feature vector.
These features are computed on-the-fly during dataset loading from the stored action and episode start time $t_{\text{start}}$, so batch files contain only rod states, actions, and $t_{\text{start}}$ without pre-computed phase data.

The full surrogate input concatenates the z-score normalized rod state $\bar{\mathbf{s}}_t$, the normalized action $\mathbf{a}_t$, and the per-element phase features:

\begin{equation}
  \mathbf{z}_t
    = \bigl[\bar{\mathbf{s}}_t \;\|\; \mathbf{a}_t \;\|\;
            \boldsymbol{\phi}_1, \ldots, \boldsymbol{\phi}_{N_e}\bigr]
    \in \mathbb{R}^{189}
  \label{eq:surrogate-input}
\end{equation}

\begin{itemize}
  \item $\bar{\mathbf{s}}_t = (\mathbf{s}_t - \boldsymbol{\mu}_s)/\boldsymbol{\sigma}_s$ --- z-score normalized state using training-set per-feature mean $\boldsymbol{\mu}_s$ and standard deviation $\boldsymbol{\sigma}_s$
  \item $\mathbf{a}_t \in [-1,1]^5$ --- normalized CPG action
  \item $\boldsymbol{\phi}_1, \ldots, \boldsymbol{\phi}_{N_e}$ --- per-element phase triplets (60 dimensions total)
\end{itemize}
The 189 dimensions decompose as $124 + 5 + 60$.

\subsection{Surrogate Model Architecture}
\label{sec:methods:architecture}

The surrogate is a feedforward MLP.
It maps the 189-dimensional input $\mathbf{z}_t$ to a 124-dimensional normalized state delta, from which the predicted next state is recovered through affine reconstruction:

\begin{equation}
  f_\theta : \mathbf{z}_t \mapsto \overline{\Delta\mathbf{s}}
  \label{eq:surrogate-forward}
\end{equation}

\begin{equation}
  \hat{\mathbf{s}}_{t+1}
    = \mathbf{s}_t
      + \boldsymbol{\sigma}_\Delta \odot f_\theta(\mathbf{z}_t)
      + \boldsymbol{\mu}_\Delta
  \label{eq:next-state-reconstruction}
\end{equation}

\begin{itemize}
  \item $f_\theta$ --- neural surrogate with parameters $\theta$
  \item $\overline{\Delta\mathbf{s}}$ --- predicted normalized state delta
  \item $\boldsymbol{\mu}_\Delta, \boldsymbol{\sigma}_\Delta \in \mathbb{R}^{124}$ --- training-set mean and standard deviation of the state delta $(\mathbf{s}_{t+1} - \mathbf{s}_t)$, computed once before training begins
\end{itemize}

Delta prediction offers two advantages over predicting the absolute next state.
First, per-step state changes are small relative to absolute values --- position deltas are typically on the order of centimeters per control step while absolute positions span meters --- so the target distribution is more concentrated and easier to fit.
Second, when $f_\theta(\mathbf{z}_t) \approx \mathbf{0}$, the reconstruction yields $\hat{\mathbf{s}}_{t+1} \approx \mathbf{s}_t + \boldsymbol{\mu}_\Delta$, providing a reasonable default (the mean empirical transition) that acts as an implicit residual connection.

The network architecture is:

\begin{equation}
  \mathbb{R}^{189}
    \xrightarrow{\text{Linear}}
    \bigl[\text{Linear}(512) \to \text{LayerNorm} \to \text{SiLU}\bigr]^{\times 3}
    \xrightarrow{\text{Linear}}
    \mathbb{R}^{124}
  \label{eq:architecture}
\end{equation}

The output layer is initialized to zero so that the model starts by predicting zero delta.
LayerNorm is applied after each linear transformation (before the SiLU activation) to stabilize training dynamics.
The total number of trainable parameters is approximately 659K.

\subsection{Training Procedure}
\label{sec:methods:training}

Training is organized in two phases that introduce the rollout loss gradually to avoid instability during early optimization.

\subsubsection{Phase 1: Single-Step MSE (Epochs 1--20)}
\label{sec:methods:training:phase1}

The model is trained to minimize the mean squared error on the normalized state delta:

\begin{equation}
  \mathcal{L}_{\text{single}}
    = \frac{1}{N} \sum_{i=1}^{N}
      \bigl\| f_\theta(\mathbf{z}_t^{(i)})
             - \overline{\Delta\mathbf{s}}_{\text{true}}^{(i)} \bigr\|^2
  \label{eq:single-loss}
\end{equation}

The sum runs over $N$ training samples, where $f_\theta(\mathbf{z}_t^{(i)})$ is the predicted normalized delta for sample $i$ and $\overline{\Delta\mathbf{s}}_{\text{true}}^{(i)}$ is the corresponding ground truth.
This phase establishes accurate one-step predictions before the more expensive rollout computation is introduced.

\subsubsection{Phase 2: Combined Loss with Rollout (Epoch 20 Onward)}
\label{sec:methods:training:phase2}

An 8-step autoregressive rollout loss is added to penalize error accumulation:

\begin{equation}
  \mathcal{L}
    = \mathcal{L}_{\text{single}}
      + \lambda_r \cdot \mathcal{L}_{\text{rollout}}
  \label{eq:combined-loss}
\end{equation}

\begin{equation}
  \mathcal{L}_{\text{rollout}}
    = \frac{1}{L} \sum_{t=0}^{L-1}
      \bigl\| \hat{\mathbf{s}}_{t+1} - \mathbf{s}_{t+1}^{\text{true}} \bigr\|^2
  \label{eq:rollout-loss}
\end{equation}

The rollout weight is $\lambda_r = 0.1$ and the rollout length is $L = 8$ steps.
At each step, the model's own prediction $\hat{\mathbf{s}}_{t+1}$ is used as input for the next, and gradients are back-propagated through the full unrolled trajectory.
This loss directly penalizes the drift between surrogate rollouts and ground-truth PyElastica trajectories --- the primary failure mode when the surrogate is used as a substitute environment for RL training.

\subsubsection{Density-Weighted Sampling}
\label{sec:methods:training:density}

Sobol quasi-random action sampling produces a non-uniform distribution over the state space.
To compensate, each training sample is weighted by the inverse of its estimated local density:

\begin{equation}
  w^{(i)} = \frac{1}{\hat{p}(\mathbf{c}^{(i)})}
  \label{eq:density-weight}
\end{equation}

\begin{equation}
  \mathbf{c}^{(i)}
    = \bigl[\bar{x}_{\text{CoM}},\;
            \bar{y}_{\text{CoM}},\;
            \|\dot{\mathbf{x}}\|,\;
            |\bar{\omega}_z|\bigr]
  \label{eq:density-features}
\end{equation}

\begin{itemize}
  \item $w^{(i)}$ --- importance weight for sample $i$
  \item $\hat{p}$ --- estimated local density (joint histogram with 20 bins per dimension)
  \item $\mathbf{c}^{(i)} \in \mathbb{R}^4$ --- summary feature capturing center-of-mass position ($\bar{x}_{\text{CoM}}$, $\bar{y}_{\text{CoM}}$), speed ($\|\dot{\mathbf{x}}\|$), and mean angular velocity magnitude ($|\bar{\omega}_z|$)
\end{itemize}
Weights are clipped at $10\times$ the mean to prevent rare samples from dominating the gradient.

\subsubsection{Hyperparameters}
\label{sec:methods:training:hyperparams}

The best configuration, identified through an architecture sweep (see \cref{sec:discussion:arch}), used: learning rate $1 \times 10^{-3}$ with the Adam optimizer (default momentum parameters), batch size 4096, and early stopping on validation MSE.
All experiments were logged to Weights \& Biases for tracking and comparison.

% ---------------------------------------------------------------------------
\section{Experiments and Results}
\label{sec:experiments}
% ---------------------------------------------------------------------------

\subsection{Surrogate Accuracy}
\label{sec:experiments:accuracy}

\placeholder{Surrogate validation against held-out PyElastica trajectories: single-step MSE, multi-step rollout RMSE, per-state-component accuracy breakdown, and the effect of per-element phase encoding on angular velocity prediction.}

\subsection{RL Training with Surrogate}
\label{sec:experiments:rl}

\placeholder{PPO training in the surrogate environment: learning curves, wall-clock time comparison with direct Elastica training, and policy performance evaluated on both surrogate and real Elastica environments.}

\subsection{Comparison with Direct Elastica RL Baseline}
\label{sec:experiments:baseline}

\placeholder{Head-to-head comparison between surrogate-trained and Elastica-trained policies: sample efficiency, wall-clock time, final policy quality, and the transfer gap attributable to surrogate modeling error.}

% ---------------------------------------------------------------------------
\section{Discussion}
\label{sec:discussion}
% ---------------------------------------------------------------------------

\subsection{Physics Calibration Challenges}
\label{sec:discussion:physics}

Establishing a reliably locomoting Cosserat rod simulation required resolving several categories of physics calibration errors.
Each issue independently prevented stable serpentine locomotion and would have rendered any surrogate trained on the resulting data physically meaningless.
These findings illustrate a general principle: a surrogate model of an incorrectly calibrated simulator will faithfully replicate the simulator's errors.

\textbf{Rod radius and bending stiffness.}
The initial rod radius was set to $r = 0.001$\,m, matching a plausible physical snake scale.
Bending stiffness, however, scales as $EI \propto r^4$: with $I = \pi r^4 / 4$, a Young's modulus of $E = 2 \times 10^6$\,Pa gave $EI \approx 1.6 \times 10^{-6}$\,N$\cdot$m$^2$ --- insufficient to drive meaningful rod deformation.
The rod was effectively limp and produced near-zero locomotion velocity.
Increasing $E$ alone caused the opposite failure: at $E \geq 10^6$\,Pa, the rod snapped violently toward the target curvature, with actual curvatures reaching $\pm 100$\,rad/m (compared to targets of $\pm 2.5$\,rad/m) and node velocities exceeding 178\,m/s, causing simulation divergence.

A grid search over $(E, r)$ pairs identified $r = 0.02$\,m with $E = 10^5$\,Pa as producing stable serpentine motion.
Increasing the radius by a factor of 20 increases $I$ by a factor of $160{,}000$, so even with a simultaneous 20-fold reduction in Young's modulus, the net bending stiffness $EI$ increased sufficiently for physically plausible deformation.
This calibration illustrates a modeling challenge specific to Cosserat rod simulators: the parameter values required to produce correct physical behavior need not correspond to the material parameters of the modeled object, particularly when the simulator's length scales and integration timesteps differ from the physical system by orders of magnitude.

\textbf{Serpenoid wave direction sign error.}
The CPG actuation module contained a sign error in the temporal term of the curvature equation.
The initial implementation computed $\kappa(s, t) = A \sin(2\pi k \cdot s - 2\pi f \cdot t + \phi)$, producing a wave with phase velocity $+f/k$ in the $+s$ direction (tail to head).
Anisotropic RFT friction generates net forward thrust only when the wave propagates posteriorly (head to tail).
The correct expression is $\kappa(s, t) = A \sin(2\pi k \cdot s + 2\pi f \cdot t + \phi)$, with phase velocity $-f/k$ in the $+s$ direction.

This bug was not detected during initial simulator construction because the training environment computed curvatures inline rather than calling the CPG module's export method.
It was identified only after observing consistent negative forward-velocity rewards across multiple training runs, which prompted a comparison of the wave equation against the serpentine gait literature.
The experience motivates explicit unit tests that verify wave propagation direction for a known CPG configuration, rather than relying on training metrics to surface physics errors.

\textbf{RFT friction coefficient tuning.}
Anisotropic drag coefficients derived from published values for soil-crawling snakes ($c_n/c_t \approx 10$) produced excessive lateral drift on the flat-surface simulation substrate.
A grid search over the ratio $c_n/c_t$ found that $c_t = 0.01$ and $c_n = 0.05$ (ratio 5:1) yielded stable forward propulsion with a 94:1 forward-to-lateral velocity ratio across the full CPG parameter range.
The discrepancy from published values reflects the different contact mechanics of dry-surface locomotion (no soil deformation, no granular interlocking), confirming that substrate-specific friction calibration is necessary when transferring the contact model across physical regimes.

\subsection{Surrogate Architecture Experiments}
\label{sec:discussion:arch}

\placeholder{Architecture sweep results: MLP width and depth comparison, residual connections, history window experiments, rollout loss ablation, and the FT-Transformer baseline. Results from Phase 3 Plans 03--05.}

\subsection{Data Collection Pipeline Challenges}
\label{sec:discussion:pipeline}

Four engineering challenges were encountered while building the parallel data collection system, each arising from the interaction between PyElastica's Numba-compiled physics kernels and Python multiprocessing.

\textbf{Numba thread pool deadlock.}
PyElastica's solver is implemented as Numba JIT-compiled functions.
When worker processes are spawned via the \texttt{forkserver} start method, they inherit Python module state but not the Numba thread pool.
On first physics evaluation, Numba re-initializes its thread pool (defaulting to 48 threads on the 48-core machine).
Under heavy load --- 16 workers each spawning 48 threads yields 768 threads competing for 48 cores --- a silent deadlock occurred at episode boundaries.
The fix was to set \texttt{NUMBA\_NUM\_THREADS=1}, eliminating Numba's internal parallelism.
This is the correct trade-off: each PyElastica instance operates on a single 20-element rod, so intra-function parallelism provides no benefit, while process-level parallelism across 16 workers provides a $16\times$ throughput improvement.

\textbf{Stall detection false positives.}
A health monitor tracks per-worker data output rates and respawns stalled workers.
During initial deployment, all 16 workers were flagged as stalled and respawned within 60 seconds of startup.
The cause was that PyElastica initialization --- forking, importing, constructing the rod, and warming the Numba JIT cache --- takes more than 60 seconds under load, exactly matching the stall threshold.
The fix was to condition the stall counter on having produced at least one transition: zero-progress during initialization is not counted.

\textbf{Parallel scaling bottleneck.}
Beyond 16 workers, throughput plateaued rather than continuing to grow linearly.
The bottleneck was memory bandwidth: PyElastica's solver performs many small array operations that are memory-bound, and at high worker counts the aggregate working sets exceed the 30\,MB shared L3 cache per socket.
The optimal configuration was 16 workers (${\sim}8$ per socket), achieving near-linear scaling.
The surrogate ultimately sidesteps this bottleneck entirely: once the dataset is collected, RL training runs on GPU at throughputs orders of magnitude higher than any CPU-bound simulation.

\textbf{Angular velocity coverage gap.}
Analysis of the V1 dataset revealed poor surrogate predictions for element angular velocity ($\omega_z$), with mean R$^2 = 0.23$ and individual elements below zero.
The state perturbation standard deviation of $\sigma_\omega = 0.05$\,rad/s was far below the operational CPG range of 1--10\,rad/s, leaving large-$|\omega_z|$ regions severely underrepresented.
For the V2 dataset, $\sigma_\omega$ was increased to 1.5\,rad/s, covering the full operational range.
This case illustrates a general issue: if the collection distribution does not cover the state-space regions that the downstream RL policy will visit, the surrogate will be inaccurate precisely where accuracy is most needed.

% ---------------------------------------------------------------------------
\section{Conclusion}
\label{sec:conclusion}
% ---------------------------------------------------------------------------

\placeholder{Write conclusion after all experimental results are available.}

% ---------------------------------------------------------------------------
% Bibliography
% ---------------------------------------------------------------------------
\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
